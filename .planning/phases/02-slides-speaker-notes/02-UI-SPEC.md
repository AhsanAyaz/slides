---
phase: 2
slug: slides-speaker-notes
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-01
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for Phase 2: Slides & Speaker Notes.
> The authoritative visual source is `talks/tedx-kth/reference-material/slides.html` — all
> per-slide CSS extracted verbatim from that mockup. Generated by gsd-ui-researcher.

---

## Design System

| Property         | Value                                                              |
|------------------|--------------------------------------------------------------------|
| Tool             | none — raw CSS in `talks/tedx-kth/style.css`                      |
| Preset           | not applicable                                                     |
| Component library| none                                                               |
| Icon library     | inline SVG (hand-drawn stroke style, no external library)          |
| Fonts            | Fraunces (serif), JetBrains Mono (mono), Inter (sans) — already declared in Phase 1 @font-face + Google Fonts CDN |

**Source:** Phase 1 established the CSS variable contract in `talks/tedx-kth/style.css` and the font stack. Phase 2 adds only per-slide layout rules. All new rules must be scoped under `body.tedx`.

---

## Spacing Scale

This is a presentation deck at 16:9 aspect ratio. Spacing is expressed as percentage-based internal padding (matching mockup) plus a supporting pixel scale for inline details.

| Token    | Value | Usage                                                 |
|----------|-------|-------------------------------------------------------|
| xs       | 4px   | Cursor blinking margin, inline gaps between meta elements |
| sm       | 8px   | Title stack gap (title-2 margin-top), meta label gap, browser-bar gap, ai-cell gap |
| xs-alt   | 12px  | name-trans margin-top, label margin-bottom, footer .sep margin, stat-label margin-top |
| md       | 16px  | Eyebrow-to-title gap, grid cell gap values, browser-url padding, .term padding horizontal |
| md-alt   | 20px  | etymology padding-top                                 |
| lg       | 24px  | Slide-4 prompt-to-command gap (32px nearest multiple) |
| xl       | 32px  | Slide-4 column gap, closing-rule width                |
| xl-alt   | 40px  | browser-content padding vertical                      |
| 2xl      | 48px  | Not used directly                                     |
| 3xl      | 64px  | Not used directly                                     |

**Per-slide internal padding (percentage-based, lifted verbatim from mockup):**

| Slide | Padding declaration                                 |
|-------|-----------------------------------------------------|
| 1     | `padding: 7% 9%`                                    |
| 2     | `align-items: center; justify-content: center`      |
| 3     | `align-items: center; justify-content: center`      |
| 4     | `align-items: center; justify-content: center; gap: 32px` |
| 5     | `padding: 1px` (grid gap: 1px between cells), inner cells: `padding: 5%` |
| 6     | `padding: 6% 8%`                                    |
| 7     | `padding: 5% 6% 6%`                                 |
| 8     | `padding: 8%`                                       |
| 9     | `padding: 8%`                                       |
| 10    | `padding: 8%`                                       |
| 11    | `padding: 10% 12%`                                  |
| 12    | `padding: 8%`                                       |

**Exceptions:**
- Touch targets not applicable (presentation, not web app).
- Slide 11 uses wider side padding (12%) to leave room for the oversized quotation mark decoration.
- Slide 5 uses 1px padding as grid-line gap, not content spacing.

---

## Typography

All sizes use `clamp()` for projector-to-laptop scaling. The values below are the **max** of each clamp range (full 16:9 at 1920px). The mockup uses exactly these four typographic roles:

| Role           | Font                  | Size (max)                     | Weight    | Line Height | Letter Spacing |
|----------------|-----------------------|--------------------------------|-----------|-------------|----------------|
| Eyebrow/label  | JetBrains Mono        | 12px (`clamp(9px, 1vw, 12px)`)  | 400       | n/a (single line) | 0.3em uppercase |
| Body/subtext   | Inter                 | 14px (fixed in browser-content) | 400       | 1.5         | normal         |
| Definition/quote | Fraunces serif      | 36px (`clamp(20px, 2.8vw, 36px)`) | 400    | 1.4         | -0.01em        |
| Display/number | Fraunces serif        | 140px (`clamp(64px, 10vw, 140px)`) | 400   | 1.0         | -0.04em        |

**Weight inventory (exactly 2 in use):**
- 400 — body, definitions, display numbers, eyebrow labels, and all Fraunces text that appears in italic (italic is a style modifier, not a weight)
- 500 — medium-emphasis Fraunces and mono text (commands, stat numbers, name display)

**Note on italic:** `font-style: italic` is a style modifier applied to weight 400 or 500 depending on context — it is not a third weight. Slide 1 `.title-2` uses Fraunces italic with the same clamp range as the heading, at weight 400, colored `var(--accent)`. All other italic uses (name-trans, `.vs`, `.footnote`, definition `.key` spans, closing `.em` span) follow the same pattern.

**Per-role details and slide-specific clamp variations:**

### Eyebrow
Default: JetBrains Mono, `clamp(9px, 1vw, 12px)`, weight 400, uppercase, letter-spacing 0.3em, `var(--ink-soft)` or `var(--ink-faint)`.

Used for: `TEDx KTH Salon · The Voice of Innovation`, section labels, source citations, `.term` labels on slides 7 and 11.

Slide-specific clamp variations:
- Slide 7/11 `.term`: same default range, letter-spacing 0.35em
- Slide 8 `.stat-label`: `clamp(11px, 1.3vw, 16px)` — slightly larger for stat context, letter-spacing 0.25em
- Slide 5 `.label-sub`: `clamp(8px, 0.9vw, 10px)` — smaller for grid cells, letter-spacing 0.1em
- Slide 6 `.name-dates`: `clamp(10px, 1.1vw, 13px)`, letter-spacing 0.1em

### Body
Default: Inter, 14px fixed (used in `.browser-content` only), weight 400, line-height 1.5.

Slide-specific clamp variations:
- Slide 2 `.subtext`: Inter 13px fixed (intentionally dimmed context content)
- Slide 2 `.heading`: Fraunces `clamp(20px, 2.4vw, 32px)`, line-height 1.2, weight 400 — this is a Body-role heading at browser-frame scale, not a canonical Display

### Definition
Default: Fraunces, `clamp(20px, 2.8vw, 36px)`, weight 400, line-height 1.4, letter-spacing -0.01em.

Used for: slide 11 `.definition`, slide 6 `.etymology`.

Slide-specific clamp variations:
- Slide 1 `.title-1` / `.title-2`: `clamp(22px, 3.4vw, 46px)`, line-height 1.15, letter-spacing -0.02em — title-2 adds `font-style: italic; color: var(--accent)` at weight 400
- Slide 7 `.vs`: `clamp(20px, 2.5vw, 32px)`, `font-style: italic`, `color: var(--accent)`, opacity 0.7
- Slide 7 `.footer-line`: Fraunces italic, `clamp(14px, 1.7vw, 22px)`, `--ink`, letter-spacing 0.02em
- Slide 6 `.name-arabic`: `clamp(26px, 3.5vw, 44px)`, weight 500, `color: var(--gold)`, line-height 1.1, letter-spacing -0.01em
- Slide 6 `.name-trans`: Fraunces italic, `clamp(14px, 1.6vw, 20px)`, `--ink-soft`, weight 400
- Slide 5 `.label-main`: Fraunces, `clamp(13px, 1.7vw, 20px)`, `--ink`, line-height 1.2
- Slide 9 `.dragon-text`: Fraunces, `clamp(28px, 4vw, 52px)`, weight 400, line-height 1.5, letter-spacing -0.02em
- Slide 10 `.word`: Fraunces, `clamp(32px, 5vw, 72px)`, weight 500, letter-spacing -0.02em
- Slide 12 `.question`: Fraunces, `clamp(40px, 6.5vw, 96px)`, weight 400, line-height 1.05, letter-spacing -0.03em

### Display
Default: Fraunces, `clamp(64px, 10vw, 140px)`, weight 400, line-height 1.0, letter-spacing -0.04em.

Used for: slide 7 `.number` (left/right num-blocks).

Slide-specific clamp variations:
- Slide 8 `.stat-num`: `clamp(72px, 11vw, 156px)`, weight 500 — slightly larger for stat slide; `.use` stat uses `color: var(--ink)`, `.trust` stat uses `color: var(--accent)`
- Slide 3 `.terminal-text`: JetBrains Mono, `clamp(28px, 4.5vw, 56px)`, weight 400 — mono display at terminal scale
- Slide 4 `.command`: JetBrains Mono, `clamp(56px, 9vw, 120px)`, weight 500 — the largest mono display element
- Slide 4 `.prompt`: JetBrains Mono, `clamp(20px, 2.5vw, 32px)`, weight 400, `color: var(--gold)`, opacity 0.7
- Slide 11 `::before` quote mark: Fraunces, `clamp(80px, 14vw, 200px)`, weight 400, `color: var(--accent)`, opacity 0.25

**`font-style: italic` usage:** slide 1 title-2, slide 6 name-trans, slide 7 `vs`, slide 10 `.footnote`, slide 11 definition `.key` spans, slide 12 `.em` span. Italic is always paired with `var(--accent)` color when used for emphasis in body copy.

---

## Color

Two-palette system established in Phase 1. Usage per slide is locked by the mockup.

Dark background slides (10 of 12) establish the dominant palette; cream (2 of 12) is the secondary; accent red appears on 9 of 12 slides in tightly scoped roles.

| Role            | Value                          | Usage                                   |
|-----------------|--------------------------------|-----------------------------------------|
| Dominant deep   | `#0a0a0a` (`--bg-deep`)        | Slides 1, 2 (`#050505`), 3, 4, 7, 8, 9, 11 backgrounds |
| Dominant cream  | `#f5f1ea` (`--bg-soft`)        | Slides 10, 12 backgrounds               |
| Slide 6 bg      | `linear-gradient(135deg, #1a1208 0%, #0a0a0a 100%)` | Slide 6 only |
| Slide 5 bg      | `#0f0f0f` (grid container) + `#1a1a1a` (cells) | Slide 5 only |
| Ink on deep     | `#f5f1ea` (`--ink`)            | All text on dark-background slides      |
| Ink on cream    | `#1a1208` (`--ink-dark`)       | All text on light-background slides     |
| Ink-soft (deep) | `rgba(245,241,234,0.6)`        | Secondary text on dark slides           |
| Ink-soft (cream)| `rgba(10,10,10,0.55)` (`--ink-dark-soft`) | Secondary text on light slides |
| Ink-faint (deep)| `rgba(245,241,234,0.25)`       | Source citations, date labels, faint rules |
| Accent          | `#e62b1e` (`--accent`)         | **See reserved-for list below**         |
| Gold            | `#c9a961` (`--gold`)           | Slide 4 `$` prompt, slide 5 SVG icon strokes, slide 6 name + etymology `.word` |

**Accent `#e62b1e` reserved exclusively for:**
1. Slide 1: the 40×4px decorative rule (`::before` pseudo), title-2 italic text
2. Slide 3: terminal text color, blinking cursor block
3. Slide 4: the `.dot` inside `adk web` (the period between `adk` and `web`)
4. Slide 7: "The Thinking Gap" label border + text, italic `vs`, footer `.sep` dots
5. Slide 8: `29%` stat number (`.trust .stat-num`)
6. Slide 9: down-arrow characters in dragon text (↓ arrows only)
7. Slide 10: `text-decoration-color` on strikethrough "typing", SVG arrow stroke (`#e62b1e` hardcoded)
8. Slide 11: oversized `"` pseudo-element (opacity 0.25), `.key` italic spans ("what", "before")
9. Slide 12: the 32×2px decorative rule (`::before`), `.em` italic "worth"

**Accent must NOT appear on:** slides 2, 5, 6 (those use gold), or anywhere not listed above.

**`--accent-soft`** (`rgba(230,43,30,0.15)`): not used in slide content; available for hover states if interactive elements are added.

---

## Per-Slide Layout Contract

Each slide section in `slides-markdown/tedx-kth.md` uses raw `<section>` HTML (the probe markdown confirmed this convention). The `data-background-color` and `class` attributes on each `<section>` drive the palette switch.

### Slide 1 — Title (`class="deep"`, `data-background-color="#0a0a0a"`)

**Layout:** `flex-direction: column; justify-content: center; align-items: flex-start; padding: 7% 9%`

**Elements:**
- `::before` pseudo: 40×4px red rule, `position: absolute; top: 6%; left: 9%`
- `.eyebrow`: mono, `clamp(9px,1vw,12px)`, `letter-spacing: 0.3em`, uppercase, `--ink-soft`, `margin-bottom: 6%`
- `.title-1`: Fraunces, `clamp(22px,3.4vw,46px)`, weight 400, `line-height: 1.15`, `letter-spacing: -0.02em`, `--ink`, `max-width: 95%`
- `.title-2`: Fraunces, same size, weight 400, `font-style: italic`, `color: var(--accent)`, `margin-top: 8px`
- `.meta`: absolute, `bottom: 6%; left: 9%; right: 9%`, flex space-between, mono, `clamp(9px,1vw,12px)`, letter-spacing 0.15em, uppercase, `--ink-soft`

**Content:**
- eyebrow: `TEDx KTH Salon · The Voice of Innovation`
- title-1: `The next billion developers won't be blocked by syntax.`
- title-2: `They'll be blocked by thinking.`
- meta left: `Muhammad Ahsan Ayaz`
- meta right: `Stockholm · 04.05.2026`

---

### Slide 2 — Demo browser (`class="deep"`, `data-background-color="#050505"`)

**Layout:** `align-items: center; justify-content: center; position: relative; overflow: hidden`

**Elements:**
- `::before` pseudo: two radial gradients (blue 6%, red 4% tint) — atmospheric depth
- `.browser-frame`: `width: 75%`, glass effect (`background: rgba(20,20,22,0.6)`, `border: 1px solid rgba(255,255,255,0.08)`, `border-radius: 8px`, `opacity: 0.45`, `backdrop-filter: blur(20px)`)
- `.browser-bar`: flex, `padding: 10px 16px`, `background: rgba(0,0,0,0.3)`, `gap: 8px`, `border-bottom: 1px solid rgba(255,255,255,0.05)`
- `.browser-dot` ×3: `10×10px`, `border-radius: 50%`, `background: rgba(255,255,255,0.12)`
- `.browser-url`: mono, `font-size: 10px`, `--ink-soft`, flex-1, `background: rgba(255,255,255,0.06)`, `padding: 4px 12px`, `border-radius: 4px`
- `.browser-content`: `padding: 40px 32px`
  - `.label`: mono, 9px, uppercase, `letter-spacing: 0.15em`, `color: rgba(66,133,244,0.7)` (Google blue tint), `margin-bottom: 12px`
  - `.heading`: Fraunces, `clamp(20px,2.4vw,32px)`, `line-height: 1.2`, `opacity: 0.7`, `margin-bottom: 16px`
  - `.subtext`: Inter, 13px, `--ink-soft`, `max-width: 70%`, `line-height: 1.5`

**Content (intentionally dimmed — audience does not read it):**
- url: `google.github.io/adk-docs`
- label: `Agent Development Kit`
- heading: `Build AI agents that can reason, plan, and act`
- subtext: `Production-grade agentic framework with multi-agent orchestration, tool use, and seamless deployment to Google Cloud.`

---

### Slide 3 — PERMISSION DENIED (`class="deep"`, `data-background-color="#0a0a0a"`)

**Layout:** `align-items: center; justify-content: center; position: relative; overflow: hidden`

**Elements:**
- `::after` pseudo (CRT scan-line): `position: absolute; inset: 0; repeating-linear-gradient(0deg, transparent 0px, transparent 2px, rgba(255,255,255,0.015) 2px, rgba(255,255,255,0.015) 3px); pointer-events: none`
- `.terminal-text`: mono, `clamp(28px,4.5vw,56px)`, `color: var(--accent)`, `letter-spacing: 0.02em`, `filter: blur(0.4px)`, `text-shadow: 0 0 30px rgba(230,43,30,0.3)`
- `.cursor`: `display: inline-block; width: 0.6em; height: 1em; background: var(--accent); margin-left: 4px; vertical-align: middle; animation: blink 1.2s infinite`
- `@keyframes blink`: `0%, 49% { opacity: 1 } 50%, 100% { opacity: 0 }` — declared in `style.css` scope

**Content:** `PERMISSION DENIED` + blinking cursor block

---

### Slide 4 — adk web (`class="deep"`, `data-background-color="#0a0a0a"`)

**Layout:** `align-items: center; justify-content: center; flex-direction: column; gap: 32px`

**Elements:**
- `.prompt`: mono, `clamp(20px,2.5vw,32px)`, `color: var(--gold)`, `opacity: 0.7`
- `.command`: mono, `clamp(56px,9vw,120px)`, weight 500, `color: var(--ink)`, `letter-spacing: -0.02em`
  - `.dot` span: `color: var(--accent)` — the dot between `adk` and `web`

**Content:** prompt `$`, command `adk<span class="dot">.</span>web`

> Note: `adk web` in the mockup HTML renders as `adk web` (space-separated), but the `.dot` class is on the space character shown as a period visually. The executor must reproduce this: `adk<span class="dot">.</span>web` where `.dot { color: var(--accent) }`.

---

### Slide 5 — 2×2 AI grid (`class="deep"`, `data-background-color="#0f0f0f"`)

**Layout:** `display: grid; grid-template-columns: 1fr 1fr; grid-template-rows: 1fr 1fr; gap: 1px; padding: 1px`

**Elements:**
- `::before` pseudo (section header): `content: 'WHAT AI ACTUALLY DID FOR ME'`, `position: absolute; top: 6%; left: 50%; transform: translateX(-50%)`, mono, `clamp(9px,1vw,12px)`, letter-spacing 0.3em, `--ink-faint`, `z-index: 10`
- 4× `.ai-cell`: `background: #1a1a1a`, flex column, `align-items: center; justify-content: center; padding: 5%; gap: 8px`
  - `.icon-wrap`: `clamp(40px,6vw,70px)` square, flex centered
  - `svg`: 100% of icon-wrap, `stroke: var(--gold); stroke-width: 1.5; fill: none; opacity: 0.8`
  - `.label-main`: Fraunces, `clamp(13px,1.7vw,20px)`, `--ink`, `line-height: 1.2`
  - `.label-sub`: mono, `clamp(8px,0.9vw,10px)`, `--ink-soft`, letter-spacing 0.1em, uppercase

**Cell content (verbatim from mockup):**

| Cell | SVG path description | label-main | label-sub |
|------|---------------------|-----------|-----------|
| Top-left | Document with folded corner, two horizontal lines | Recovered a hacked blog | A week of work · one evening |
| Top-right | Document rectangle with four horizontal lines | Replaced a CMS with markdown | $6 / month · sovereignty |
| Bottom-left | Document with header bar, five content lines | Wrote a book in 3 months | Friction removed · thinking kept |
| Bottom-right | Chat bubble with three dots | A marketing agent called Alkhwarizmi | A teacher in my pocket |

**SVG viewBox:** all `0 0 64 64`. Exact path data lifted verbatim from `slides.html` lines 1028–1076.

---

### Slide 6 — Al-Khwarizmi (`class="deep"`, `data-background-color="#0a0a0a"`)

> Note: slide 6 uses a gradient background on the element itself, not `data-background-color`. The `data-background-color` should be `#0a0a0a` (darkest end of gradient) to avoid a mismatch flash.

**Layout:** `display: flex; align-items: center; padding: 6% 8%; position: relative; overflow: hidden`

**Elements:**
- `::before` pseudo: `radial-gradient(circle at 25% 50%, rgba(201,169,97,0.08) 0%, transparent 50%)` — gold atmospheric glow
- `.manuscript-area`: `flex: 0 0 38%; aspect-ratio: 3/4; background: #2a1d0a; border: 1px solid rgba(201,169,97,0.3); position: relative; overflow: hidden`
  - Inner `::before`: `position: absolute; inset: 8%; border: 1px solid rgba(201,169,97,0.2)` — inner frame rule
  - `.arabic-deco` SVG: `width: 60%; opacity: 0.5; color: var(--gold)` — decorative geometry
- `.text-area`: `flex: 1; padding-left: 8%; z-index: 1`
  - `.name-arabic`: Fraunces, `clamp(26px,3.5vw,44px)`, weight 500, `color: var(--gold)`, `line-height: 1.1`, `letter-spacing: -0.01em`
  - `.name-trans`: Fraunces italic, `clamp(14px,1.6vw,20px)`, `--ink-soft`, `margin-top: 12px`, `line-height: 1.4`
  - `.name-dates`: mono, `clamp(10px,1.1vw,13px)`, `--ink-faint`, `margin-top: 16px`, `letter-spacing: 0.1em`
  - `.etymology`: `margin-top: 32px; padding-top: 20px; border-top: 1px solid rgba(201,169,97,0.2)`, Fraunces, `clamp(13px,1.5vw,18px)`, `--ink`, `line-height: 1.5`, `max-width: 90%`
    - `.word` span: `color: var(--gold); font-style: italic`

**Manuscript SVG:** exact `viewBox="0 0 200 260"` geometry from mockup lines 1093–1108. Two diamond/rhombus shapes with nested diamonds and center circles, a horizontal rule pair, and small italic caption "House of Wisdom · Baghdad". Color via `currentColor` on gold element.

**Content:**
- name-arabic: `Muḥammad ibn Mūsā al-Khwārizmī` (two lines, `<br>`)
- name-trans: `whose name became <em>algorithm</em>`
- name-dates: `c. 780 — 850 CE`
- etymology: `<span class="word">al-jabr</span>: completion, balancing,<br>putting the broken parts back together.`

---

### Slide 7 — The Thinking Gap (`class="deep"`, `data-background-color="#0a0a0a"`)

**Layout:** `display: flex; flex-direction: column; justify-content: center; padding: 5% 6% 6%; position: relative`

**Elements:**
- `.v3-label` (top center): `text-align: center; margin-bottom: 4%`
  - `.term` span: mono, `clamp(10px,1.2vw,14px)`, letter-spacing 0.35em, uppercase, `color: var(--accent)`, `padding: 8px 16px; border: 1px solid var(--accent); border-radius: 2px`
- `.row`: `display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; gap: 5%`
  - `.num-block.left`: center, `.number` Fraunces `clamp(64px,10vw,140px)` weight 400 `color: var(--ink-soft)`, `.label` mono `clamp(10px,1.1vw,13px)` uppercase letter-spacing 0.2em `--ink-soft` `margin-top: 16px`
  - `.vs`: Fraunces italic, `clamp(20px,2.5vw,32px)`, `color: var(--accent)`, `opacity: 0.7`
  - `.num-block.right`: same as left but `.number` uses `color: var(--ink)` (full bright)
- `.footer-line`: `margin-top: 5%; text-align: center`, Fraunces italic, `clamp(14px,1.7vw,22px)`, `--ink`, `letter-spacing: 0.02em`
  - `.sep` span: `color: var(--accent); margin: 0 12px`

**Content:**
- term: `The Thinking Gap`
- left number: `9`, label: `users`
- vs: `vs`
- right number: `~ 4,000`, label: `users`
- footer: `Same year<span class="sep">·</span>Same hands<span class="sep">·</span>Same AI`

---

### Slide 8 — 84%/29% (`class="deep"`, `data-background-color="#0a0a0a"`)

**Layout:** `display: flex; flex-direction: column; justify-content: center; padding: 8%`

**Elements:**
- `.stat-row`: `display: grid; grid-template-columns: 1fr 1fr; gap: 8%; margin-bottom: 6%`
  - `.stat-block.use`: `.stat-num` Fraunces `clamp(72px,11vw,156px)` weight 500 `color: var(--ink)`, `%` suffix in `font-size: 0.5em; color: var(--ink-soft)`, `.stat-label` mono `clamp(11px,1.3vw,16px)` uppercase letter-spacing 0.25em `--ink-soft` `margin-top: 12px`
  - `.stat-block.trust`: identical but `.stat-num` uses `color: var(--accent)`
- `.source`: mono, `clamp(9px,1vw,11px)`, `--ink-faint`, letter-spacing 0.1em, `text-align: right`

**Content:**
- use stat: `84<span style="font-size:0.5em;color:var(--ink-soft)">%</span>`, label: `use AI tools`
- trust stat: `29<span style="font-size:0.5em;color:var(--ink-soft)">%</span>`, label: `trust them`
- source: `Stack Overflow Developer Survey · 2025 · n = 49,000+`

---

### Slide 9 — The dragon (`class="deep"`, `data-background-color="#0a0a0a"`)

**Layout:** `display: flex; align-items: center; justify-content: center; padding: 8%; position: relative`

**Elements:**
- `.dragon-text`: Fraunces, `clamp(28px,4vw,52px)`, weight 400, `line-height: 1.5`, `color: var(--ink)`, `text-align: left`, `letter-spacing: -0.02em`
  - 4× `.row` spans: `display: block; margin-bottom: 8px`
    - `.arrow-up` span: `color: var(--ink); font-weight: 500`
    - `.arrow-down` span: `color: var(--accent); font-weight: 500`

**Content (verbatim):**
```
<span class="row">Use <span class="arrow-up">↑</span></span>
<span class="row">Trust <span class="arrow-down">↓</span></span>
<span class="row">Output <span class="arrow-up">↑</span></span>
<span class="row">Demand <span class="arrow-down">↓</span></span>
```

---

### Slide 10 — Karpathy reframe (`class="cream"`, `data-background-color="#f5f1ea"`)

**Layout:** `display: flex; align-items: center; justify-content: center; padding: 8%; position: relative`

**Elements:**
- `.reframe`: `display: flex; align-items: center; gap: clamp(40px,7vw,100px)`
  - `.word-box.from`:
    - `.word`: Fraunces, `clamp(32px,5vw,72px)`, weight 500, `letter-spacing: -0.02em`, `color: var(--ink-dark-soft)`, `text-decoration: line-through; text-decoration-color: var(--accent); text-decoration-thickness: 2px`
    - `.label`: mono, `clamp(9px,1vw,12px)`, uppercase, letter-spacing 0.25em, `--ink-dark-soft`, `margin-top: 12px`
  - `.arrow-svg`: `width: clamp(60px,10vw,140px); height: auto; flex-shrink: 0`
    - SVG `viewBox="0 0 200 60"`: horizontal line + arrowhead, `stroke: #e62b1e; stroke-width: 2; stroke-linecap: round`
    - Exact path: `M10 30 L180 30` + `M165 18 L185 30 L165 42`
  - `.word-box.to`:
    - `.word`: same Fraunces size, weight 500, `color: var(--ink-dark)` (no strikethrough)
    - `.label`: `still ours`
- `.footnote`: `position: absolute; bottom: 6%; right: 8%`, Fraunces italic, `clamp(11px,1.2vw,14px)`, `--ink-dark-soft`

**Content:**
- from word: `typing`, label: `automated`
- arrow SVG (hardcoded in markup)
- to word: `thinking`, label: `still ours`
- footnote: `paraphrasing Karpathy`

---

### Slide 11 — Context engineering definition (`class="deep"`, `data-background-color="#0a0a0a"`)

**Layout:** `display: flex; align-items: center; justify-content: center; padding: 10% 12%; position: relative`

**Elements:**
- `::before` pseudo (oversized opening quote): `content: '"'`, `position: absolute; top: 4%; left: 8%`, Fraunces, `clamp(80px,14vw,200px)`, `color: var(--accent)`, `opacity: 0.25`, `line-height: 0.8`
- `.term`: `position: absolute; top: 8%; left: 50%; transform: translateX(-50%)`, mono, `clamp(9px,1vw,12px)`, letter-spacing 0.3em, uppercase, `--ink-soft`
- `.definition`: Fraunces, `clamp(20px,2.8vw,36px)`, weight 400, `line-height: 1.4`, `color: var(--ink)`, `text-align: center`, `max-width: 95%`, `letter-spacing: -0.01em`
  - `.key` span: `color: var(--accent); font-style: italic`

**Content:**
- term: `Context engineering`
- definition: `Be deliberate about <span class="key">what</span> you put in front of an intelligent system <span class="key">before</span> you ask it to do anything.`

---

### Slide 12 — Closing question (`class="cream"`, `data-background-color="#f5f1ea"`)

**Layout:** `display: flex; align-items: center; justify-content: center; padding: 8%; position: relative`

**Elements:**
- `::before` pseudo (accent rule): `position: absolute; top: 8%; left: 50%; transform: translateX(-50%)`, `width: 32px; height: 2px; background: var(--accent)`
- `.question`: Fraunces, `clamp(40px,6.5vw,96px)`, weight 400, `line-height: 1.05`, `color: var(--ink-dark)`, `text-align: center`, `letter-spacing: -0.03em`, `max-width: 95%`
  - `.em` span: `font-style: italic; color: var(--accent)`

**Content:** `What is <span class="em">worth</span><br>building?`

---

## Animations & Special Effects

All animations are CSS-only and scoped under `body.tedx`.

| Slide | Effect | Implementation |
|-------|--------|----------------|
| 3 | Blinking terminal cursor | `@keyframes blink { 0%,49%{opacity:1} 50%,100%{opacity:0} }` at `animation: blink 1.2s infinite` on `.cursor` |
| 3 | CRT scan-line texture | `::after` pseudo with `repeating-linear-gradient(0deg, transparent 0px, transparent 2px, rgba(255,255,255,0.015) 2px, rgba(255,255,255,0.015) 3px)` |
| 3 | Terminal text glow | `filter: blur(0.4px); text-shadow: 0 0 30px rgba(230,43,30,0.3)` |

No other animations. The deck is intentionally still — visual motion would compete with the speaker's narrative.

---

## Reveal.js Markdown Conventions

| Convention | Value |
|------------|-------|
| File | `slides-markdown/tedx-kth.md` |
| Horizontal separator | `^\n---\n$` (blank line, three dashes, blank line) |
| Vertical separator | `^\n--\n$` (not used — all 12 slides are horizontal) |
| Slide content method | Raw `<section>` HTML blocks — NOT pure markdown headings |
| Background color | `data-background-color` attribute on each `<section>` |
| Palette class | `class="deep"` or `class="cream"` on each `<section>` |
| Notes | Reveal.js Notes plugin syntax: `<aside class="notes">...</aside>` inside each `<section>` |

**Palette per slide:**

| Slide | class | data-background-color |
|-------|-------|-----------------------|
| 1     | deep  | `#0a0a0a`             |
| 2     | deep  | `#050505`             |
| 3     | deep  | `#0a0a0a`             |
| 4     | deep  | `#0a0a0a`             |
| 5     | deep  | `#0f0f0f`             |
| 6     | deep  | `#0a0a0a`             |
| 7     | deep  | `#0a0a0a`             |
| 8     | deep  | `#0a0a0a`             |
| 9     | deep  | `#0a0a0a`             |
| 10    | cream | `#f5f1ea`             |
| 11    | deep  | `#0a0a0a`             |
| 12    | cream | `#f5f1ea`             |

---

## Speaker Notes Contract (NOTES-01, NOTES-02)

Each `<aside class="notes">` block carries: timing cue · narrative beat · stage direction.

Source: `talks/tedx-kth/reference-material/slides.md` and mockup `.slide-notes` blocks.

| Slide | Timing | Notes content (verbatim from mockup) |
|-------|--------|--------------------------------------|
| 1  | Cold open · 0:00 | This is the official TEDx-published title. Hold on this slide for ~10 seconds before the first line, while the room settles. The audience reads the title; you do not say it aloud. Your opening line ("I want to tell you about four hours of my life I will never get back") lands while this slide is up. The signature line — "The typing has been automated. The thinking has not." — is delivered spoken-only at minute ~19, not on a slide. |
| 2  | ~ 0:30 | The browser is dimmed and slightly out of focus on purpose. The audience does not need to read this slide. It is just establishing visual context for the demo story. Advance to slide 3 the moment you say "permission denied." |
| 3  | ~ 1:30 | Stay on this slide for the entire "generate, paste, deploy, wait, fail" sequence. The visual stillness while you describe motion is what makes the contrast hit. The slight blur and CRT-scan-line texture imply exhaustion without showing it. |
| 4  | ~ 3:00 | This slide should appear AFTER you say "I just needed to type three letters and a word into my terminal." The reveal is the punchline. Don't anticipate it. Hold the silence for two full seconds after the slide changes, then deliver the new "narrate their thoughts" line: "And I know what some of you are thinking right now. How does a senior engineer waste four hours like that?" Stay on this slide through that beat before continuing. |
| 5  | ~ 5:00 | Each cell of the grid corresponds to one beat in the spoken passage. Reveal the slide all at once when you start the "AI has given me superpowers" passage. The hand-drawn icon style is intentional — it signals that these are personal, hand-built things, not enterprise software. Hold this slide through the new loop opener: "And honestly, that should be the end of the talk... But that is not why I am standing here." |
| 6  | ~ 7:00 | Reveal this slide as you say his full name. Pause for two full seconds after "He has been working for you your whole life and you didn't know it." The geometric pattern echoes Islamic manuscript art without depicting any specific copyrighted artwork. The etymology line at the bottom returns later in Act 5 as a callback. |
| 7  | HEART OF TALK · ~ 9:00 | UPDATED IN V3 with the branded term at the top. The label "The Thinking Gap" is what makes this concept portable. Reveal the numbers first when you say "Some of them have nine"; the term label is already visible. After you say "I started calling this the thinking gap," point to the label briefly so the audience associates the words with the visual. The 9 is muted; the ~4,000 is bright. Hold this slide all the way through the new "narrate their thoughts" line: "Now you might be thinking what I thought, the first time I noticed the gap..." |
| 8  | ~ 11:30 | Two numbers, side by side, no chart. The 84% is the same color as the rest of the slide; the 29% is in TEDx red. The visual contrast IS the argument. Source line is small enough to be quoted accurately by anyone who screenshots, but not loud enough to compete with the numbers. |
| 9  | ~ 12:30 | Each line lands as you say it. If you have a clicker that supports staged reveal, animate them in one at a time. Otherwise, all four appear together. The asymmetric arrows do the work. The slide is intentionally text-only and quiet — this is the dragon section, the talk's lowest emotional point. Visual stillness amplifies it. |
| 10 | ~ 14:00 | Theme switch from dark to light. The shift in palette is intentional — it signals the talk has turned from diagnosis to insight. The strikethrough on "typing" is in TEDx red. This slide carries the new sharper negative-frame line: "And if you skip it, the speed becomes the trap. The AI will help you get to the wrong answer faster than you have ever gotten there in your life." |
| 11 | ~ 15:30 | Definition slide. The oversized opening quotation mark in the upper-left visually frames the line as something you are reading aloud, even though it is your own definition. The two italicized red words ("what" and "before") are the anchor points the audience should remember. |
| 12 | ~ 19:00 | The final slide. The clip. The poster. Reveal this when you say "the question is what is worth building." Stay on it through the entire close, the silence, and the walk-off. The signature line — "The typing has been automated. The thinking has not." — is delivered spoken-only just before the final beat, with no slide change. Do NOT advance to a "Thank you" or contact slide. The talk ends on this question, and the lights coming back up is the punctuation. |

**Special directions preserved (NOTES-02):**
- Slide 1: "Hold on this slide for ~10 seconds before the first line"
- Slide 4: "Hold the silence for two full seconds after the slide changes"
- Slide 7: "Hold this slide all the way through the new 'narrate their thoughts' line"
- Slide 12: "Do NOT advance to a 'Thank you' or contact slide"

---

## CSS Scoping Contract

All new CSS rules added in Phase 2 follow these rules (carried over from Phase 1 decision):

1. Every selector begins with `body.tedx` — no bare class or element selectors.
2. Per-slide classes (`.slide-1` through `.slide-12`) are nested under `body.tedx .reveal .slides section`.
3. The `@keyframes blink` animation must be declared once in `talks/tedx-kth/style.css` — not inline in the markdown.
4. Inline `style=""` attributes are permitted for one-off overrides (e.g., the `%` suffix size on stat numbers) where extracting to a class would add noise.
5. The `::before` / `::after` pseudo-elements on slides cannot be reproduced in pure Reveal.js markdown — they require the `<section>` to have an explicit CSS class applied, which is declared in style.css.

---

## Copywriting Contract

This is a presentation, not a web app. Copywriting contract covers the fixed text on each slide and the notes.

| Element | Copy |
|---------|------|
| Slide 1 eyebrow | `TEDx KTH Salon · The Voice of Innovation` |
| Slide 1 title-1 | `The next billion developers won't be blocked by syntax.` |
| Slide 1 title-2 | `They'll be blocked by thinking.` |
| Slide 1 meta-left | `Muhammad Ahsan Ayaz` |
| Slide 1 meta-right | `Stockholm · 04.05.2026` |
| Slide 2 browser url | `google.github.io/adk-docs` |
| Slide 2 browser label | `Agent Development Kit` |
| Slide 2 browser heading | `Build AI agents that can reason, plan, and act` |
| Slide 3 terminal | `PERMISSION DENIED` (+ blinking cursor) |
| Slide 4 prompt | `$` |
| Slide 4 command | `adk web` (the dot between rendered as `.dot` span in accent) |
| Slide 5 header | `WHAT AI ACTUALLY DID FOR ME` |
| Slide 5 cell labels | See per-slide layout table above |
| Slide 6 name | `Muḥammad ibn Mūsā al-Khwārizmī` |
| Slide 6 name-trans | `whose name became algorithm` |
| Slide 6 dates | `c. 780 — 850 CE` |
| Slide 6 etymology | `al-jabr: completion, balancing, putting the broken parts back together.` |
| Slide 7 term | `The Thinking Gap` |
| Slide 7 numbers | `9` (left, muted) · `~ 4,000` (right, bright) |
| Slide 7 labels | `users` (both sides) |
| Slide 7 footer | `Same year · Same hands · Same AI` (dots in accent) |
| Slide 8 stats | `84%` (ink) · `29%` (accent) |
| Slide 8 labels | `use AI tools` · `trust them` |
| Slide 8 source | `Stack Overflow Developer Survey · 2025 · n = 49,000+` |
| Slide 9 dragon | `Use ↑` / `Trust ↓` / `Output ↑` / `Demand ↓` |
| Slide 10 from | `typing` (strikethrough) · label: `automated` |
| Slide 10 to | `thinking` · label: `still ours` |
| Slide 10 footnote | `paraphrasing Karpathy` |
| Slide 11 term | `Context engineering` |
| Slide 11 definition | `Be deliberate about what you put in front of an intelligent system before you ask it to do anything.` (with "what" and "before" in italic accent spans) |
| Slide 12 question | `What is worth building?` (with "worth" in italic accent) |
| Primary CTA | n/a — presentation deck, no interactive CTA |
| Empty state | n/a |
| Error state | n/a |
| Destructive confirmation | n/a |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

No external component registries. All slide components are bespoke HTML+CSS authored inline in the markdown file.

---

## Pre-Population Audit

| Source | Decisions Used |
|--------|---------------|
| `talks/tedx-kth/reference-material/slides.html` | All 12 per-slide CSS rules, color values, typography sizes, layout declarations, SVG geometry, animation keyframes |
| `talks/tedx-kth/reference-material/slides.md` | All 12 speaker notes texts, timing cues, stage directions, special delivery instructions |
| `talks/tedx-kth/style.css` (Phase 1) | CSS variable names, font stacks, two-palette switch, scoping convention |
| `talks/tedx-kth.html` | Reveal.js separator convention, plugin list, `body.tedx` class, markdown file path |
| `talks/slides-markdown/tedx-kth.md` | Confirmed `<section>` HTML authoring pattern, inline style usage |
| REQUIREMENTS.md | SLIDE-01–12, STYLE-04, NOTES-01, NOTES-02 success criteria |
| ROADMAP.md | Success criteria confirming slide order, special effects per slide |
| User input | 0 (all design decisions pre-populated from upstream artifacts) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
