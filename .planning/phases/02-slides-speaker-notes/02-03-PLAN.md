---
phase: 02-slides-speaker-notes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - talks/slides-markdown/tedx-kth.md
  - talks/tedx-kth/style.css
autonomous: true
gap_closure: true
closes_gaps:
  - CR-01
  - CR-02
requirements:
  - STYLE-04
  - SLIDE-01
  - SLIDE-02
  - SLIDE-03
  - SLIDE-04
  - SLIDE-05
  - SLIDE-06
  - SLIDE-07
  - SLIDE-08
  - SLIDE-09
  - SLIDE-10
  - SLIDE-11
  - SLIDE-12
  - NOTES-01
  - NOTES-02

must_haves:
  truths:
    - "Pressing the right-arrow key advances through all 12 slides as horizontal positions — not vertical sub-slides"
    - "The chat-bubble icon in slide-5 cell 4 shows three visible dots (cx=24, cx=32, cx=40, cy=27, r=1.5)"
    - "Each of the 12 slides carries its CSS class (slide-1 through slide-12) and background color via the comment directive, not via a nested <section> tag"
  artifacts:
    - path: "talks/slides-markdown/tedx-kth.md"
      provides: "12-slide deck with comment directives — no explicit <section> wrapper tags"
      contains: "<!-- .slide:"
    - path: "talks/tedx-kth/style.css"
      provides: "fill:currentColor override for slide-5 chat-bubble circles"
      contains: "section.slide-5 .ai-cell svg circle"
  key_links:
    - from: "talks/slides-markdown/tedx-kth.md"
      to: "reveal.js markdown plugin (plugin/markdown/plugin.js)"
      via: "<!-- .slide: class='...' data-background-color='...' --> comment directive"
      pattern: "<!-- .slide:"
    - from: "talks/tedx-kth/style.css"
      to: "slide-5 cell-4 chat-bubble <circle> elements"
      via: "body.tedx .reveal .slides section.slide-5 .ai-cell svg circle { fill: currentColor; }"
      pattern: "ai-cell svg circle"
---

<objective>
Close two structural blockers that prevent the TEDx deck from rendering correctly as 12 horizontal slides.

CR-01: All 12 slides are wrapped in explicit `<section>` tags that the reveal.js markdown plugin turns into nested vertical sub-slides. Replace every opening `<section data-background-color="..." class="...">` and closing `</section>` with the `<!-- .slide: class="..." data-background-color="..." -->` comment directive at the top of each separator-delimited segment.

CR-02: CSS `fill: none` on `.ai-cell svg` (style.css line 457) overrides `fill="currentColor"` on the three `<circle>` elements in slide-5 cell 4's chat-bubble icon. Add one targeted rule after that block to restore `fill: currentColor` for circles only.

Purpose: Unblock all 12 slide requirements (SLIDE-01 through SLIDE-12) and STYLE-04. NOTES-01 and NOTES-02 are already satisfied; this plan ensures the structural fix does not disturb them.

Output: A correctly structured tedx-kth.md (no `<section>` wrapper tags, 12 comment directives present) and an updated style.css with the circle fill-override rule.
</objective>

<execution_context>
@/Users/amu1o5/personal/slides/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amu1o5/personal/slides/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/amu1o5/personal/slides/.planning/ROADMAP.md
@/Users/amu1o5/personal/slides/.planning/phases/02-slides-speaker-notes/02-UI-SPEC.md
@/Users/amu1o5/personal/slides/.planning/phases/02-slides-speaker-notes/02-VERIFICATION.md

<interfaces>
<!-- Reveal.js markdown plugin comment-directive syntax (confirmed from plugin source) -->
<!--
  Place this as the FIRST line of a separator-delimited segment (no blank line before it
  within the segment, blank lines before/after the --- separators remain unchanged):

  <!-- .slide: class="CLASSES" data-background-color="COLOR" -->

  The plugin reads this and adds the attributes to the outer <section data-markdown> wrapper
  it creates for this segment. No nested <section> is created.
-->

<!-- Per-slide directive values (exact — do not alter class names or colors) -->
<!-- Slide  1: <!-- .slide: class="deep slide-1"  data-background-color="#0a0a0a" --> -->
<!-- Slide  2: <!-- .slide: class="deep slide-2"  data-background-color="#050505" --> -->
<!-- Slide  3: <!-- .slide: class="deep slide-3"  data-background-color="#0a0a0a" --> -->
<!-- Slide  4: <!-- .slide: class="deep slide-4"  data-background-color="#0a0a0a" --> -->
<!-- Slide  5: <!-- .slide: class="deep slide-5"  data-background-color="#0f0f0f" --> -->
<!-- Slide  6: <!-- .slide: class="deep slide-6"  data-background-color="#0a0a0a" --> -->
<!-- Slide  7: <!-- .slide: class="deep slide-7"  data-background-color="#0a0a0a" --> -->
<!-- Slide  8: <!-- .slide: class="deep slide-8"  data-background-color="#0a0a0a" --> -->
<!-- Slide  9: <!-- .slide: class="deep slide-9"  data-background-color="#0a0a0a" --> -->
<!-- Slide 10: <!-- .slide: class="cream slide-10" data-background-color="#f5f1ea" --> -->
<!-- Slide 11: <!-- .slide: class="deep slide-11" data-background-color="#0a0a0a" --> -->
<!-- Slide 12: <!-- .slide: class="cream slide-12" data-background-color="#f5f1ea" --> -->

<!-- CSS rule to add in style.css immediately after the closing brace of the
     body.tedx .reveal .slides section.slide-5 .ai-cell svg { ... } block (line 459): -->
<!--
body.tedx .reveal .slides section.slide-5 .ai-cell svg circle {
  fill: currentColor;
}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace explicit section tags with comment directives in tedx-kth.md (CR-01)</name>
  <files>talks/slides-markdown/tedx-kth.md</files>

  <read_first>
    - talks/slides-markdown/tedx-kth.md — read the entire file (270 lines) to see the current state of all 12 <section> wrappers, all <aside class="notes"> blocks, all SVG content, and the --- separators before making any edits.
  </read_first>

  <action>
    The file currently has 12 separator-delimited segments. Each segment starts with an explicit
    `<section data-background-color="..." class="...">` opening tag and ends with `</section>`.

    Perform the following replacement for all 12 slides. The transformation rule is:

    BEFORE (opening tag — varies per slide, occupies line 1 of each segment):
      `<section data-background-color="COLOR" class="CLASSES">`

    AFTER (comment directive — first line of the segment, no blank line before it within the segment):
      `<!-- .slide: class="CLASSES" data-background-color="COLOR" -->`

    Note the attribute ORDER swap: the comment directive puts `class` before `data-background-color`.
    The values are identical — only the element form changes.

    BEFORE (closing tag — last line of each segment, after the </aside> tag):
      `</section>`

    AFTER: Delete this line entirely. No replacement content.

    Apply this transformation to all 12 slides using these exact directive values:

    Slide 1  (was: `<section data-background-color="#0a0a0a" class="deep slide-1">`):
      `<!-- .slide: class="deep slide-1" data-background-color="#0a0a0a" -->`

    Slide 2  (was: `<section data-background-color="#050505" class="deep slide-2">`):
      `<!-- .slide: class="deep slide-2" data-background-color="#050505" -->`

    Slide 3  (was: `<section data-background-color="#0a0a0a" class="deep slide-3">`):
      `<!-- .slide: class="deep slide-3" data-background-color="#0a0a0a" -->`

    Slide 4  (was: `<section data-background-color="#0a0a0a" class="deep slide-4">`):
      `<!-- .slide: class="deep slide-4" data-background-color="#0a0a0a" -->`

    Slide 5  (was: `<section data-background-color="#0f0f0f" class="deep slide-5">`):
      `<!-- .slide: class="deep slide-5" data-background-color="#0f0f0f" -->`

    Slide 6  (was: `<section data-background-color="#0a0a0a" class="deep slide-6">`):
      `<!-- .slide: class="deep slide-6" data-background-color="#0a0a0a" -->`

    Slide 7  (was: `<section data-background-color="#0a0a0a" class="deep slide-7">`):
      `<!-- .slide: class="deep slide-7" data-background-color="#0a0a0a" -->`

    Slide 8  (was: `<section data-background-color="#0a0a0a" class="deep slide-8">`):
      `<!-- .slide: class="deep slide-8" data-background-color="#0a0a0a" -->`

    Slide 9  (was: `<section data-background-color="#0a0a0a" class="deep slide-9">`):
      `<!-- .slide: class="deep slide-9" data-background-color="#0a0a0a" -->`

    Slide 10 (was: `<section data-background-color="#f5f1ea" class="cream slide-10">`):
      `<!-- .slide: class="cream slide-10" data-background-color="#f5f1ea" -->`

    Slide 11 (was: `<section data-background-color="#0a0a0a" class="deep slide-11">`):
      `<!-- .slide: class="deep slide-11" data-background-color="#0a0a0a" -->`

    Slide 12 (was: `<section data-background-color="#f5f1ea" class="cream slide-12">`):
      `<!-- .slide: class="cream slide-12" data-background-color="#f5f1ea" -->`

    PRESERVE without any change:
    - All `---` separator lines and their surrounding blank lines
    - All `<aside class="notes">` blocks and their content (timing cues, stage directions)
    - All div elements and their class names (eyebrow, title-1, title-2, meta, browser-frame, terminal-text, ai-cell, manuscript-area, etc.)
    - All SVG elements including path, circle, line, rect, text elements
    - All inline styles and fill="currentColor" / fill="none" SVG attributes
    - The `fill="none"` on the SVG paths in slide 6 (those are correct — they use stroke only)
    - All `<span>` elements, `<em>` elements, and `<br>` tags

    DO NOT add any blank line between the comment directive and the first content element of the segment.
    The comment directive must be the very first line of each segment (immediately after the blank line that follows `---`).

    After edits, the file must have:
    - Zero lines matching `<section` (no opening section tags at all)
    - Zero lines matching `</section>` (no closing section tags at all)
    - Exactly 12 lines matching `<!-- .slide:` (one per slide)
    - Exactly 12 `<aside class="notes">` blocks (unchanged)
    - Exactly 11 `---` separator lines (unchanged)
  </action>

  <verify>
    <automated>
      # Zero opening section tags
      grep -c "^&lt;section" talks/slides-markdown/tedx-kth.md && echo "FAIL: section tags remain" || echo "PASS: no section tags"

      # Confirm with raw grep (no html encoding):
      grep -n "<section" talks/slides-markdown/tedx-kth.md | wc -l
      # Expected: 0

      grep -n "</section>" talks/slides-markdown/tedx-kth.md | wc -l
      # Expected: 0

      grep -c "<!-- .slide:" talks/slides-markdown/tedx-kth.md
      # Expected: 12

      grep -c "aside class=\"notes\"" talks/slides-markdown/tedx-kth.md
      # Expected: 12

      grep -c "^---$" talks/slides-markdown/tedx-kth.md
      # Expected: 11

      # Verify all 12 exact directives are present:
      grep 'class="deep slide-1"' talks/slides-markdown/tedx-kth.md
      grep 'class="deep slide-2"' talks/slides-markdown/tedx-kth.md
      grep 'class="cream slide-10"' talks/slides-markdown/tedx-kth.md
      grep 'class="cream slide-12"' talks/slides-markdown/tedx-kth.md

      # Verify circle fill attributes in slide-5 SVG are still present (not accidentally removed):
      grep 'fill="currentColor"' talks/slides-markdown/tedx-kth.md | wc -l
      # Expected: at least 8 (3 circles in slide-5, several in slide-6 svg, slide-6 text)
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "<section" talks/slides-markdown/tedx-kth.md` returns 0
    - `grep -c "</section>" talks/slides-markdown/tedx-kth.md` returns 0
    - `grep -c "<!-- .slide:" talks/slides-markdown/tedx-kth.md` returns 12
    - `grep -c 'aside class="notes"' talks/slides-markdown/tedx-kth.md` returns 12
    - `grep -c "^---$" talks/slides-markdown/tedx-kth.md` returns 11
    - `grep '<!-- .slide: class="deep slide-1" data-background-color="#0a0a0a"' talks/slides-markdown/tedx-kth.md` exits 0
    - `grep '<!-- .slide: class="deep slide-5" data-background-color="#0f0f0f"' talks/slides-markdown/tedx-kth.md` exits 0
    - `grep '<!-- .slide: class="cream slide-10" data-background-color="#f5f1ea"' talks/slides-markdown/tedx-kth.md` exits 0
    - `grep '<!-- .slide: class="cream slide-12" data-background-color="#f5f1ea"' talks/slides-markdown/tedx-kth.md` exits 0
    - `grep 'fill="currentColor"' talks/slides-markdown/tedx-kth.md` returns at least 8 matches (SVG circle and path attributes preserved)
    - `grep "Cold open" talks/slides-markdown/tedx-kth.md` exits 0 (slide-1 notes preserved)
    - `grep "Do NOT advance" talks/slides-markdown/tedx-kth.md` exits 0 (slide-12 special direction preserved)
  </acceptance_criteria>

  <done>tedx-kth.md has exactly 12 <!-- .slide: --> comment directives, zero explicit section tags, all 12 aside.notes blocks intact, and all 11 --- separators unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add fill:currentColor override for slide-5 chat-bubble circles in style.css (CR-02)</name>
  <files>talks/tedx-kth/style.css</files>

  <read_first>
    - talks/tedx-kth/style.css — read lines 450–470 to confirm the exact closing brace location of the `body.tedx .reveal .slides section.slide-5 .ai-cell svg { ... }` block (currently ends at line 459 with `}`). This establishes the insertion point for the new rule.
  </read_first>

  <action>
    The current block at lines 452–459 of style.css is:

    ```css
    body.tedx .reveal .slides section.slide-5 .ai-cell svg {
      width: 100%;
      height: 100%;
      stroke: var(--gold);
      stroke-width: 1.5;
      fill: none;
      opacity: 0.8;
    }
    ```

    Insert the following new rule immediately after the closing `}` of this block (after line 459),
    separated by one blank line:

    ```css
    body.tedx .reveal .slides section.slide-5 .ai-cell svg circle {
      fill: currentColor;
    }
    ```

    This rule is more specific than the `.ai-cell svg` rule (adds `circle` element selector),
    so it wins the cascade and restores `fill: currentColor` on the three dot circles in cell 4's
    chat-bubble icon. The `fill: none` on paths, rects, and lines in cells 1–3 is unaffected.

    DO NOT modify any other rule in style.css.
    DO NOT change the `.ai-cell svg` block itself.
    DO NOT alter any other selector, property, or value.
  </action>

  <verify>
    <automated>
      grep -n "ai-cell svg circle" talks/tedx-kth/style.css
      # Expected: one match with `fill: currentColor;` on the following line

      grep -A2 "ai-cell svg circle" talks/tedx-kth/style.css
      # Expected output contains: fill: currentColor;

      # Verify the original fill:none rule on .ai-cell svg is still present and unchanged:
      grep -A6 "\.ai-cell svg {" talks/tedx-kth/style.css | grep "fill: none"
      # Expected: one match
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "ai-cell svg circle" talks/tedx-kth/style.css` returns 1
    - `grep -A2 "ai-cell svg circle" talks/tedx-kth/style.css` output includes the line `  fill: currentColor;`
    - `grep -n "fill: none" talks/tedx-kth/style.css` still returns a match on the `.ai-cell svg` block (original rule preserved)
    - The new rule appears immediately after line 459 (the closing `}` of the `.ai-cell svg` block) — verify with `grep -n "ai-cell svg circle" talks/tedx-kth/style.css` returning a line number in the range 460–465
  </acceptance_criteria>

  <done>style.css contains the circle fill override rule immediately after the .ai-cell svg block, and the original fill:none rule on .ai-cell svg is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored markdown → reveal.js plugin | Static file read by JS at page load; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | tedx-kth.md comment directives | accept | Static file served over local HTTP or file://; no user-controlled input; content is authored by the developer |
| T-02-03-02 | Information Disclosure | style.css | accept | Public CSS file for a conference presentation; no secrets or PII |
</threat_model>

<verification>
After both tasks complete, run the following to confirm gap closure:

```bash
# CR-01 closed: zero section tags, 12 directives
grep -c "<section" talks/slides-markdown/tedx-kth.md   # must be 0
grep -c "<!-- .slide:" talks/slides-markdown/tedx-kth.md  # must be 12

# CR-02 closed: circle override present
grep -c "ai-cell svg circle" talks/tedx-kth/style.css   # must be 1
grep -A2 "ai-cell svg circle" talks/tedx-kth/style.css  # must show fill: currentColor

# Notes integrity: no speaker notes lost
grep -c 'aside class="notes"' talks/slides-markdown/tedx-kth.md  # must be 12
grep "Do NOT advance" talks/slides-markdown/tedx-kth.md           # must return slide-12 direction
grep "Hold on this slide" talks/slides-markdown/tedx-kth.md       # must return slide-1 direction
grep "Hold the silence" talks/slides-markdown/tedx-kth.md         # must return slide-4 direction
grep "narrate their thoughts" talks/slides-markdown/tedx-kth.md   # must return slide-7 direction
```
</verification>

<success_criteria>
- `grep -c "<section" talks/slides-markdown/tedx-kth.md` returns 0
- `grep -c "<!-- .slide:" talks/slides-markdown/tedx-kth.md` returns 12
- `grep -c "ai-cell svg circle" talks/tedx-kth/style.css` returns 1
- All 12 `<aside class="notes">` blocks intact with their timing cues
- All four NOTES-02 special directions (slides 1, 4, 7, 12) still present verbatim
- No other CSS rules or slide content altered
</success_criteria>

<output>
After completion, create `.planning/phases/02-slides-speaker-notes/02-03-SUMMARY.md`
</output>
