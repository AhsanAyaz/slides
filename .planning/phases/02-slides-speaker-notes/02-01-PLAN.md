---
phase: 02-slides-speaker-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - talks/tedx-kth/style.css
autonomous: true
requirements: [STYLE-04, SLIDE-01, SLIDE-02, SLIDE-03, SLIDE-04, SLIDE-05, SLIDE-06, SLIDE-07, SLIDE-08, SLIDE-09, SLIDE-10, SLIDE-11, SLIDE-12]

must_haves:
  truths:
    - "talks/tedx-kth/style.css contains @keyframes blink with correct 0%,49%/50%,100% opacity steps"
    - "All 12 .slide-N class blocks are present in style.css under body.tedx .reveal .slides section selectors"
    - "Pseudo-elements ::before / ::after for slides 1, 2, 3, 5, 6, 11, 12 are declared (the browser cannot render them from markdown alone)"
    - "No rule uses a bare selector — every rule begins body.tedx"
    - "Slide 10 and 12 cream-palette rules correctly reference --ink-dark and --ink-dark-soft (not --ink)"
  artifacts:
    - path: "talks/tedx-kth/style.css"
      provides: "All 12 per-slide layout rules + @keyframes blink appended after Phase 1 rules"
      contains: "@keyframes blink"
    - path: "talks/tedx-kth/style.css"
      provides: "Slide 3 CRT scan-line ::after pseudo-element"
      contains: "repeating-linear-gradient"
    - path: "talks/tedx-kth/style.css"
      provides: "Slide 11 oversized opening quote ::before pseudo-element"
      contains: "content: '\"'"
  key_links:
    - from: "talks/tedx-kth/style.css"
      to: "talks/slides-markdown/tedx-kth.md"
      via: "CSS classes referenced by Plan 02 HTML — .slide-1 through .slide-12 must exist before the browser renders any <section> with those classes"
      pattern: "\\.slide-[0-9]+"
    - from: "talks/tedx-kth/style.css"
      to: "body.tedx CSS variable contract (Phase 1)"
      via: "All per-slide rules rely on --accent, --ink, --ink-soft, --ink-faint, --gold, --serif, --mono, --sans — declared in Phase 1 block and must remain untouched"
      pattern: "var\\(--"
---

<objective>
Append all 12 per-slide CSS layout rules — plus the `@keyframes blink` animation — to `talks/tedx-kth/style.css`.

Purpose: The markdown `<section>` blocks authored in Plan 02 reference CSS classes (.slide-1 through .slide-12, .browser-frame, .ai-cell, .dragon-text, etc.). Those classes must exist in style.css before the deck renders or the browser shows unstyled content. Plan 01 lays the CSS foundation; Plan 02 lays the content on top of it.

Output: `talks/tedx-kth/style.css` extended with ~300 lines of scoped, pixel-faithful per-slide rules that reproduce the static mockup at `talks/tedx-kth/reference-material/slides.html` exactly. No new files are created.
</objective>

<execution_context>
@/Users/amu1o5/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amu1o5/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-slides-speaker-notes/02-UI-SPEC.md

<interfaces>
<!-- Phase 1 CSS variable contract already in talks/tedx-kth/style.css.
     All per-slide rules MUST use these exact variable names. -->

body.tedx {
  --bg-deep:        #0a0a0a;
  --bg-soft:        #f5f1ea;
  --ink:            #f5f1ea;          /* overridden per-section by .deep / .cream */
  --ink-soft:       rgba(245, 241, 234, 0.6);
  --ink-faint:      rgba(245, 241, 234, 0.25);
  --ink-dark:       #0a0a0a;
  --ink-dark-soft:  rgba(10, 10, 10, 0.55);
  --accent:         #e62b1e;
  --accent-soft:    rgba(230, 43, 30, 0.15);
  --gold:           #c9a961;
  --serif:          'Fraunces', Georgia, serif;
  --mono:           'JetBrains Mono', 'Courier New', monospace;
  --sans:           'Inter', sans-serif;
}

/* Palette switch already declared: */
body.tedx .reveal .slides section.deep  { --ink: #f5f1ea; --ink-soft: rgba(245,241,234,0.6); --ink-faint: rgba(245,241,234,0.25); }
body.tedx .reveal .slides section.cream { --ink: #1a1208; --ink-soft: rgba(26,18,8,0.55);    --ink-faint: rgba(26,18,8,0.25); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append per-slide CSS (slides 1–6) + @keyframes blink to style.css</name>

  <read_first>
    - talks/tedx-kth/style.css (read current file end — append AFTER the last @font-face block, never before Phase 1 rules)
    - talks/tedx-kth/reference-material/slides.html (lines 144–560: CSS source for slides 1–6, exact values)
    - .planning/phases/02-slides-speaker-notes/02-UI-SPEC.md (per-slide layout contract, sections Slide 1 – Slide 6)
  </read_first>

  <files>talks/tedx-kth/style.css</files>

  <action>
Open talks/tedx-kth/style.css and APPEND the following block verbatim after the last @font-face declaration. Do not modify any existing content. Every selector begins with `body.tedx .reveal .slides section` to honour the CSS scoping contract (D-02).

```css
/* ============================================================
 * Phase 2 — Per-slide layout rules (SLIDE-01 – SLIDE-12)
 * All rules scoped under body.tedx per D-02.
 * Values lifted verbatim from talks/tedx-kth/reference-material/slides.html
 * ============================================================ */

/* ---------- Animation (slide 3 cursor) ---------- */
@keyframes blink {
  0%, 49% { opacity: 1; }
  50%, 100% { opacity: 0; }
}

/* ============================================
 * SLIDE 1: Title
 * ============================================ */
body.tedx .reveal .slides section.slide-1 {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: flex-start;
  padding: 7% 9%;
  position: relative;
}

body.tedx .reveal .slides section.slide-1::before {
  content: '';
  position: absolute;
  top: 6%;
  left: 9%;
  width: 40px;
  height: 4px;
  background: var(--accent);
}

body.tedx .reveal .slides section.slide-1 .eyebrow {
  font-family: var(--mono);
  font-size: clamp(9px, 1vw, 12px);
  letter-spacing: 0.3em;
  color: var(--ink-soft);
  text-transform: uppercase;
  margin-bottom: 6%;
}

body.tedx .reveal .slides section.slide-1 .title-1 {
  font-family: var(--serif);
  font-size: clamp(22px, 3.4vw, 46px);
  font-weight: 400;
  line-height: 1.15;
  letter-spacing: -0.02em;
  color: var(--ink);
  max-width: 95%;
}

body.tedx .reveal .slides section.slide-1 .title-2 {
  font-family: var(--serif);
  font-size: clamp(22px, 3.4vw, 46px);
  font-weight: 400;
  font-style: italic;
  line-height: 1.15;
  letter-spacing: -0.02em;
  color: var(--accent);
  max-width: 95%;
  margin-top: 8px;
}

body.tedx .reveal .slides section.slide-1 .meta {
  position: absolute;
  bottom: 6%;
  left: 9%;
  right: 9%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-family: var(--mono);
  font-size: clamp(9px, 1vw, 12px);
  letter-spacing: 0.15em;
  color: var(--ink-soft);
  text-transform: uppercase;
}

/* ============================================
 * SLIDE 2: Demo browser
 * ============================================ */
body.tedx .reveal .slides section.slide-2 {
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

body.tedx .reveal .slides section.slide-2::before {
  content: '';
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 30% 50%, rgba(66, 133, 244, 0.06) 0%, transparent 40%),
    radial-gradient(circle at 70% 50%, rgba(234, 67, 53, 0.04) 0%, transparent 40%);
}

body.tedx .reveal .slides section.slide-2 .browser-frame {
  width: 75%;
  background: rgba(20, 20, 22, 0.6);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 8px;
  overflow: hidden;
  opacity: 0.45;
  backdrop-filter: blur(20px);
}

body.tedx .reveal .slides section.slide-2 .browser-bar {
  display: flex;
  align-items: center;
  padding: 10px 16px;
  background: rgba(0, 0, 0, 0.3);
  gap: 6px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

body.tedx .reveal .slides section.slide-2 .browser-dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.12);
}

body.tedx .reveal .slides section.slide-2 .browser-url {
  margin-left: 12px;
  flex: 1;
  background: rgba(255, 255, 255, 0.06);
  padding: 4px 12px;
  border-radius: 4px;
  font-family: var(--mono);
  font-size: 10px;
  color: var(--ink-soft);
}

body.tedx .reveal .slides section.slide-2 .browser-content {
  padding: 40px 30px;
  min-height: 200px;
}

body.tedx .reveal .slides section.slide-2 .browser-content .label {
  font-family: var(--mono);
  font-size: 9px;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: rgba(66, 133, 244, 0.7);
  margin-bottom: 12px;
}

body.tedx .reveal .slides section.slide-2 .browser-content .heading {
  font-family: var(--serif);
  font-size: clamp(20px, 2.4vw, 32px);
  color: var(--ink);
  line-height: 1.2;
  margin-bottom: 16px;
  opacity: 0.7;
}

body.tedx .reveal .slides section.slide-2 .browser-content .subtext {
  font-family: var(--sans);
  font-size: 13px;
  color: var(--ink-soft);
  max-width: 70%;
  line-height: 1.5;
}

/* ============================================
 * SLIDE 3: PERMISSION DENIED
 * ============================================ */
body.tedx .reveal .slides section.slide-3 {
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

body.tedx .reveal .slides section.slide-3::after {
  content: '';
  position: absolute;
  inset: 0;
  background:
    repeating-linear-gradient(
      0deg,
      transparent 0px,
      transparent 2px,
      rgba(255, 255, 255, 0.015) 2px,
      rgba(255, 255, 255, 0.015) 3px
    );
  pointer-events: none;
}

body.tedx .reveal .slides section.slide-3 .terminal-text {
  font-family: var(--mono);
  font-size: clamp(28px, 4.5vw, 56px);
  color: var(--accent);
  letter-spacing: 0.02em;
  filter: blur(0.4px);
  text-shadow: 0 0 30px rgba(230, 43, 30, 0.3);
}

body.tedx .reveal .slides section.slide-3 .cursor {
  display: inline-block;
  width: 0.6em;
  height: 1em;
  background: var(--accent);
  margin-left: 4px;
  vertical-align: middle;
  animation: blink 1.2s infinite;
}

/* ============================================
 * SLIDE 4: adk web reveal
 * ============================================ */
body.tedx .reveal .slides section.slide-4 {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 32px;
}

body.tedx .reveal .slides section.slide-4 .prompt {
  font-family: var(--mono);
  font-size: clamp(20px, 2.5vw, 32px);
  color: var(--gold);
  opacity: 0.7;
}

body.tedx .reveal .slides section.slide-4 .command {
  font-family: var(--mono);
  font-size: clamp(56px, 9vw, 120px);
  font-weight: 500;
  color: var(--ink);
  letter-spacing: -0.02em;
}

body.tedx .reveal .slides section.slide-4 .command .dot {
  color: var(--accent);
}

/* ============================================
 * SLIDE 5: 2×2 AI grid
 * ============================================ */
body.tedx .reveal .slides section.slide-5 {
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: 1fr 1fr;
  gap: 1px;
  padding: 1px;
  position: relative;
}

body.tedx .reveal .slides section.slide-5::before {
  content: 'WHAT AI ACTUALLY DID FOR ME';
  position: absolute;
  top: 6%;
  left: 50%;
  transform: translateX(-50%);
  font-family: var(--mono);
  font-size: clamp(9px, 1vw, 12px);
  letter-spacing: 0.3em;
  color: var(--ink-faint);
  z-index: 10;
  white-space: nowrap;
}

body.tedx .reveal .slides section.slide-5 .ai-cell {
  background: #1a1a1a;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 5%;
  text-align: center;
  gap: 10px;
}

body.tedx .reveal .slides section.slide-5 .ai-cell .icon-wrap {
  width: clamp(40px, 6vw, 70px);
  height: clamp(40px, 6vw, 70px);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 4px;
}

body.tedx .reveal .slides section.slide-5 .ai-cell svg {
  width: 100%;
  height: 100%;
  stroke: var(--gold);
  stroke-width: 1.5;
  fill: none;
  opacity: 0.8;
}

body.tedx .reveal .slides section.slide-5 .ai-cell .label-main {
  font-family: var(--serif);
  font-size: clamp(13px, 1.7vw, 20px);
  color: var(--ink);
  line-height: 1.2;
}

body.tedx .reveal .slides section.slide-5 .ai-cell .label-sub {
  font-family: var(--mono);
  font-size: clamp(8px, 0.9vw, 10px);
  color: var(--ink-soft);
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

/* ============================================
 * SLIDE 6: Al-Khwarizmi
 * ============================================ */
body.tedx .reveal .slides section.slide-6 {
  background: linear-gradient(135deg, #1a1208 0%, #0a0a0a 100%) !important;
  display: flex;
  align-items: center;
  padding: 6% 8%;
  position: relative;
  overflow: hidden;
}

body.tedx .reveal .slides section.slide-6::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    radial-gradient(circle at 25% 50%, rgba(201, 169, 97, 0.08) 0%, transparent 50%);
}

body.tedx .reveal .slides section.slide-6 .manuscript-area {
  flex: 0 0 38%;
  aspect-ratio: 3 / 4;
  background: #2a1d0a;
  border: 1px solid rgba(201, 169, 97, 0.3);
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

body.tedx .reveal .slides section.slide-6 .manuscript-area::before {
  content: '';
  position: absolute;
  inset: 8%;
  border: 1px solid rgba(201, 169, 97, 0.2);
}

body.tedx .reveal .slides section.slide-6 .arabic-deco {
  width: 60%;
  height: auto;
  opacity: 0.5;
  color: var(--gold);
}

body.tedx .reveal .slides section.slide-6 .text-area {
  flex: 1;
  padding-left: 8%;
  z-index: 1;
}

body.tedx .reveal .slides section.slide-6 .name-arabic {
  font-family: var(--serif);
  font-size: clamp(26px, 3.5vw, 44px);
  font-weight: 500;
  color: var(--gold);
  line-height: 1.1;
  letter-spacing: -0.01em;
}

body.tedx .reveal .slides section.slide-6 .name-trans {
  font-family: var(--serif);
  font-style: italic;
  font-size: clamp(14px, 1.6vw, 20px);
  color: var(--ink-soft);
  margin-top: 12px;
  line-height: 1.4;
}

body.tedx .reveal .slides section.slide-6 .name-dates {
  font-family: var(--mono);
  font-size: clamp(10px, 1.1vw, 13px);
  color: var(--ink-faint);
  margin-top: 16px;
  letter-spacing: 0.1em;
}

body.tedx .reveal .slides section.slide-6 .etymology {
  margin-top: 32px;
  padding-top: 20px;
  border-top: 1px solid rgba(201, 169, 97, 0.2);
  font-family: var(--serif);
  font-size: clamp(13px, 1.5vw, 18px);
  color: var(--ink);
  line-height: 1.5;
  max-width: 90%;
}

body.tedx .reveal .slides section.slide-6 .etymology .word {
  color: var(--gold);
  font-style: italic;
}
```
  </action>

  <verify>
    <automated>grep -c "@keyframes blink" talks/tedx-kth/style.css && grep -c "slide-6" talks/tedx-kth/style.css && grep -c "slide-3" talks/tedx-kth/style.css</automated>
  </verify>

  <acceptance_criteria>
    - `grep "@keyframes blink" talks/tedx-kth/style.css` → exactly 1 match
    - `grep "slide-1" talks/tedx-kth/style.css` → at least 5 matches (rule blocks for .slide-1 elements)
    - `grep "slide-6" talks/tedx-kth/style.css` → at least 8 matches
    - `grep "animation: blink" talks/tedx-kth/style.css` → 1 match
    - `grep "repeating-linear-gradient" talks/tedx-kth/style.css` → 1 match (slide 3 CRT)
    - `grep "body.tedx" talks/tedx-kth/style.css | grep "slide-" | wc -l` → 0 bare slide- selectors (all scoped)
    - No existing Phase 1 content modified (git diff shows only appended lines, no deletions above the Phase 2 comment block)
  </acceptance_criteria>

  <done>Slides 1–6 CSS rules plus @keyframes blink appended to talks/tedx-kth/style.css; all selectors begin body.tedx; no Phase 1 content disturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Append per-slide CSS (slides 7–12) to style.css</name>

  <read_first>
    - talks/tedx-kth/style.css (verify Task 1 output is present at end of file before appending)
    - talks/tedx-kth/reference-material/slides.html (lines 548–886: CSS source for slides 7–12, exact values)
    - .planning/phases/02-slides-speaker-notes/02-UI-SPEC.md (per-slide layout contract, sections Slide 7 – Slide 12)
  </read_first>

  <files>talks/tedx-kth/style.css</files>

  <action>
Append the following block AFTER the slide-6 rules written in Task 1. Read the end of the current file first to confirm the slide-6 block is present and to find the correct append point.

```css
/* ============================================
 * SLIDE 7: The Thinking Gap
 * ============================================ */
body.tedx .reveal .slides section.slide-7 {
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 5% 6% 6%;
  position: relative;
}

body.tedx .reveal .slides section.slide-7 .v3-label {
  text-align: center;
  margin-bottom: 4%;
}

body.tedx .reveal .slides section.slide-7 .v3-label .term {
  display: inline-block;
  font-family: var(--mono);
  font-size: clamp(10px, 1.2vw, 14px);
  letter-spacing: 0.35em;
  color: var(--accent);
  text-transform: uppercase;
  padding: 6px 18px;
  border: 1px solid var(--accent);
  border-radius: 2px;
}

body.tedx .reveal .slides section.slide-7 .row {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  gap: 5%;
}

body.tedx .reveal .slides section.slide-7 .num-block {
  text-align: center;
}

body.tedx .reveal .slides section.slide-7 .num-block .number {
  font-family: var(--serif);
  font-size: clamp(64px, 10vw, 140px);
  font-weight: 400;
  line-height: 1;
  letter-spacing: -0.04em;
}

body.tedx .reveal .slides section.slide-7 .num-block.left .number {
  color: var(--ink-soft);
}

body.tedx .reveal .slides section.slide-7 .num-block.right .number {
  color: var(--ink);
}

body.tedx .reveal .slides section.slide-7 .num-block .label {
  font-family: var(--mono);
  font-size: clamp(10px, 1.1vw, 13px);
  text-transform: uppercase;
  letter-spacing: 0.2em;
  color: var(--ink-soft);
  margin-top: 16px;
}

body.tedx .reveal .slides section.slide-7 .vs {
  font-family: var(--serif);
  font-style: italic;
  font-size: clamp(20px, 2.5vw, 32px);
  color: var(--accent);
  opacity: 0.7;
}

body.tedx .reveal .slides section.slide-7 .footer-line {
  margin-top: 5%;
  text-align: center;
  font-family: var(--serif);
  font-style: italic;
  font-size: clamp(14px, 1.7vw, 22px);
  color: var(--ink);
  letter-spacing: 0.02em;
}

body.tedx .reveal .slides section.slide-7 .footer-line .sep {
  color: var(--accent);
  margin: 0 12px;
}

/* ============================================
 * SLIDE 8: 84% / 29%
 * ============================================ */
body.tedx .reveal .slides section.slide-8 {
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 8%;
}

body.tedx .reveal .slides section.slide-8 .stat-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8%;
  margin-bottom: 6%;
}

body.tedx .reveal .slides section.slide-8 .stat-block {
  display: flex;
  flex-direction: column;
}

body.tedx .reveal .slides section.slide-8 .stat-num {
  font-family: var(--serif);
  font-size: clamp(72px, 11vw, 156px);
  font-weight: 500;
  line-height: 1;
  letter-spacing: -0.04em;
}

body.tedx .reveal .slides section.slide-8 .stat-block.use .stat-num {
  color: var(--ink);
}

body.tedx .reveal .slides section.slide-8 .stat-block.trust .stat-num {
  color: var(--accent);
}

body.tedx .reveal .slides section.slide-8 .stat-label {
  font-family: var(--mono);
  font-size: clamp(11px, 1.3vw, 16px);
  text-transform: uppercase;
  letter-spacing: 0.25em;
  color: var(--ink-soft);
  margin-top: 12px;
}

body.tedx .reveal .slides section.slide-8 .source {
  font-family: var(--mono);
  font-size: clamp(9px, 1vw, 11px);
  color: var(--ink-faint);
  letter-spacing: 0.1em;
  text-align: right;
  margin-top: auto;
}

/* ============================================
 * SLIDE 9: The dragon
 * ============================================ */
body.tedx .reveal .slides section.slide-9 {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 8%;
  position: relative;
}

body.tedx .reveal .slides section.slide-9 .dragon-text {
  font-family: var(--serif);
  font-size: clamp(28px, 4vw, 52px);
  font-weight: 400;
  line-height: 1.5;
  color: var(--ink);
  text-align: left;
  letter-spacing: -0.02em;
}

body.tedx .reveal .slides section.slide-9 .dragon-text .arrow-up {
  color: var(--ink);
  font-weight: 600;
}

body.tedx .reveal .slides section.slide-9 .dragon-text .arrow-down {
  color: var(--accent);
  font-weight: 600;
}

body.tedx .reveal .slides section.slide-9 .dragon-text .row {
  display: block;
  margin-bottom: 8px;
}

/* ============================================
 * SLIDE 10: Karpathy reframe (cream)
 * ============================================ */
body.tedx .reveal .slides section.slide-10 {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 8%;
  position: relative;
}

body.tedx .reveal .slides section.slide-10 .reframe {
  display: flex;
  align-items: center;
  gap: clamp(40px, 7vw, 100px);
}

body.tedx .reveal .slides section.slide-10 .word-box {
  text-align: center;
}

body.tedx .reveal .slides section.slide-10 .word-box .word {
  font-family: var(--serif);
  font-size: clamp(32px, 5vw, 72px);
  font-weight: 500;
  letter-spacing: -0.02em;
  color: var(--ink-dark);
  line-height: 1;
}

body.tedx .reveal .slides section.slide-10 .word-box.from .word {
  color: var(--ink-dark-soft);
  text-decoration: line-through;
  text-decoration-color: var(--accent);
  text-decoration-thickness: 2px;
}

body.tedx .reveal .slides section.slide-10 .word-box .label {
  font-family: var(--mono);
  font-size: clamp(9px, 1vw, 12px);
  text-transform: uppercase;
  letter-spacing: 0.25em;
  color: var(--ink-dark-soft);
  margin-top: 12px;
}

body.tedx .reveal .slides section.slide-10 .arrow-svg {
  width: clamp(60px, 10vw, 140px);
  height: auto;
  flex-shrink: 0;
}

body.tedx .reveal .slides section.slide-10 .footnote {
  position: absolute;
  bottom: 6%;
  right: 8%;
  font-family: var(--serif);
  font-style: italic;
  font-size: clamp(11px, 1.2vw, 14px);
  color: var(--ink-dark-soft);
}

/* ============================================
 * SLIDE 11: Context engineering definition
 * ============================================ */
body.tedx .reveal .slides section.slide-11 {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 10% 12%;
  position: relative;
}

body.tedx .reveal .slides section.slide-11::before {
  content: '"';
  position: absolute;
  top: 4%;
  left: 8%;
  font-family: var(--serif);
  font-size: clamp(80px, 14vw, 200px);
  color: var(--accent);
  opacity: 0.25;
  line-height: 0.8;
}

body.tedx .reveal .slides section.slide-11 .term {
  position: absolute;
  top: 8%;
  left: 50%;
  transform: translateX(-50%);
  font-family: var(--mono);
  font-size: clamp(9px, 1vw, 12px);
  letter-spacing: 0.3em;
  color: var(--ink-soft);
  text-transform: uppercase;
  white-space: nowrap;
}

body.tedx .reveal .slides section.slide-11 .definition {
  font-family: var(--serif);
  font-size: clamp(20px, 2.8vw, 36px);
  font-weight: 400;
  line-height: 1.4;
  color: var(--ink);
  text-align: center;
  max-width: 95%;
  letter-spacing: -0.01em;
}

body.tedx .reveal .slides section.slide-11 .definition .key {
  color: var(--accent);
  font-style: italic;
}

/* ============================================
 * SLIDE 12: Closing question (cream)
 * ============================================ */
body.tedx .reveal .slides section.slide-12 {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 8%;
  position: relative;
}

body.tedx .reveal .slides section.slide-12::before {
  content: '';
  position: absolute;
  top: 8%;
  left: 50%;
  transform: translateX(-50%);
  width: 32px;
  height: 2px;
  background: var(--accent);
}

body.tedx .reveal .slides section.slide-12 .question {
  font-family: var(--serif);
  font-size: clamp(40px, 6.5vw, 96px);
  font-weight: 400;
  line-height: 1.05;
  color: var(--ink-dark);
  text-align: center;
  letter-spacing: -0.03em;
  max-width: 95%;
}

body.tedx .reveal .slides section.slide-12 .question .em {
  font-style: italic;
  color: var(--accent);
}
```
  </action>

  <verify>
    <automated>grep -c "slide-12" talks/tedx-kth/style.css && grep -c "slide-7" talks/tedx-kth/style.css && grep -c "slide-11::before" talks/tedx-kth/style.css</automated>
  </verify>

  <acceptance_criteria>
    - `grep "slide-7" talks/tedx-kth/style.css` → at least 8 matches
    - `grep "slide-8" talks/tedx-kth/style.css` → at least 5 matches
    - `grep "slide-9" talks/tedx-kth/style.css` → at least 4 matches
    - `grep "slide-10" talks/tedx-kth/style.css` → at least 6 matches
    - `grep "slide-11" talks/tedx-kth/style.css` → at least 4 matches
    - `grep "slide-12" talks/tedx-kth/style.css` → at least 3 matches
    - `grep "slide-11::before" talks/tedx-kth/style.css` → 1 match (oversized quote pseudo)
    - `grep "slide-12::before" talks/tedx-kth/style.css` → 1 match (accent rule pseudo)
    - `grep "ink-dark-soft" talks/tedx-kth/style.css | grep "slide-10"` → at least 2 matches (slide 10 cream-palette rules)
    - All 12 slide blocks present: `for i in $(seq 1 12); do grep -q "slide-$i" talks/tedx-kth/style.css && echo "slide-$i OK"; done`
  </acceptance_criteria>

  <done>All 12 per-slide CSS blocks written to talks/tedx-kth/style.css; every selector begins body.tedx; @keyframes blink declared once; Phase 1 content untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem | style.css is a static asset — no user input, no network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | talks/tedx-kth/style.css | accept | Static local file written by the executor; no network ingress path; risk is editing error only — mitigated by grep-based acceptance criteria verifying key rules are present |
| T-02-01-02 | Denial of Service | CSS animation (blink) | accept | Single `@keyframes blink` at 1.2s interval on one element; no performance risk on modern GPU-composited browser |
| T-02-01-03 | Spoofing | CSS scoping (body.tedx) | mitigate | Every selector scoped under `body.tedx` per D-02 — prevents rules leaking into other talks on the same page; verified by grep in acceptance criteria |
</threat_model>

<verification>
After both tasks complete, run:

```bash
# Confirm all 12 slide blocks present
for i in $(seq 1 12); do grep -q "slide-$i" talks/tedx-kth/style.css && echo "slide-$i OK" || echo "slide-$i MISSING"; done

# Confirm @keyframes declared exactly once
grep -c "@keyframes blink" talks/tedx-kth/style.css   # expected: 1

# Confirm no bare (unscoped) slide selectors
grep "\.slide-[0-9]" talks/tedx-kth/style.css | grep -v "body\.tedx"   # expected: 0 lines

# Confirm Phase 1 variables still present (not overwritten)
grep -c "\-\-accent" talks/tedx-kth/style.css   # expected: >= 20
```
</verification>

<success_criteria>
- All 12 `.slide-N` class blocks exist in `talks/tedx-kth/style.css` scoped under `body.tedx .reveal .slides section`
- `@keyframes blink` declared exactly once
- Pseudo-element rules present: slide-1::before (red rule), slide-2::before (gradient), slide-3::after (CRT scan-line), slide-5::before (grid header), slide-6::before (gold glow), slide-11::before (oversized quote), slide-12::before (accent bar)
- No Phase 1 content (CSS variables, palette switch, counter-rules, @font-face) modified
- `talks/slides-markdown/tedx-kth.md` can reference all CSS classes in Plan 02 and the browser will resolve them
</success_criteria>

<output>
After completion, create `.planning/phases/02-slides-speaker-notes/02-01-SUMMARY.md`
</output>
