---
phase: 02-slides-speaker-notes
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - talks/slides-markdown/tedx-kth.md
autonomous: true
requirements: [SLIDE-01, SLIDE-02, SLIDE-03, SLIDE-04, SLIDE-05, SLIDE-06, SLIDE-07, SLIDE-08, SLIDE-09, SLIDE-10, SLIDE-11, SLIDE-12, NOTES-01, NOTES-02]

must_haves:
  truths:
    - "Pressing the right-arrow key advances through exactly 12 slides in the order: Title → Demo browser → PERMISSION DENIED → adk web → 2x2 AI grid → Al-Khwarizmi → Thinking Gap → 84%/29% stat → Dragon → Karpathy reframe → Context engineering → Closing question"
    - "Each slide's <section> carries the correct class (deep/cream) and data-background-color attribute matching the UI-SPEC palette table"
    - "Pressing s opens the Notes window and every slide shows its timing cue, narrative beat, and stage direction"
    - "Special directions are preserved: slide 1 hold-for-10s, slide 4 hold-silence-2s, slide 7 narrate-their-thoughts, slide 12 do-not-advance"
    - "The two Phase 1 probe sections are completely replaced — no DECK · PROBE content remains in the file"
    - "TEDx red accent appears only on the elements specified: slide 1 title-2 and ::before rule, slide 3 terminal text + cursor, slide 4 .dot, slide 7 term border + .vs + .sep, slide 8 trust .stat-num, slide 9 .arrow-down, slide 10 strikethrough + SVG arrow, slide 11 ::before + .key spans, slide 12 ::before + .em"
  artifacts:
    - path: "talks/slides-markdown/tedx-kth.md"
      provides: "12-slide TEDx deck content with speaker notes"
      contains: "PERMISSION DENIED"
    - path: "talks/slides-markdown/tedx-kth.md"
      provides: "Slide 1 title content"
      contains: "The next billion developers won't be blocked by syntax."
    - path: "talks/slides-markdown/tedx-kth.md"
      provides: "Slide 12 closing question"
      contains: "What is"
    - path: "talks/slides-markdown/tedx-kth.md"
      provides: "Speaker notes for all 12 slides"
      contains: "aside class=\"notes\""
  key_links:
    - from: "talks/slides-markdown/tedx-kth.md"
      to: "talks/tedx-kth/style.css"
      via: "Each <section class=\"slide-N ...\"> references CSS classes written in Plan 01 — slide-1 through slide-12 plus component classes (ai-cell, dragon-text, etc.) must all be defined"
      pattern: "class=\"slide-[0-9]+"
    - from: "talks/slides-markdown/tedx-kth.md"
      to: "reveal.js Notes plugin"
      via: "<aside class=\"notes\"> inside each <section> — RevealNotes plugin loaded in talks/tedx-kth.html reads this element"
      pattern: "<aside class=\"notes\">"
    - from: "talks/slides-markdown/tedx-kth.md"
      to: "talks/tedx-kth.html data-separator"
      via: "Horizontal slides separated by blank-line --- blank-line (^\\n---\\n$); no vertical slides used"
      pattern: "^---$"
---

<objective>
Replace the two Phase 1 probe `<section>` blocks in `talks/slides-markdown/tedx-kth.md` with the complete 12-slide deck — all HTML content, SVG geometry, inline styles, and `<aside class="notes">` speaker notes blocks.

Purpose: This is the primary deliverable of Phase 2. After Plan 01 wrote the CSS that the slides depend on, this plan authors the actual slide content. The result is a complete, self-contained markdown file that reveal.js renders into the 12-slide TEDx deck matching the visual contract in `talks/tedx-kth/reference-material/slides.html`.

Output: `talks/slides-markdown/tedx-kth.md` — complete replacement. The file is written in its entirety (the current 16-line probe file is replaced wholesale). All 12 slides + speaker notes included.
</objective>

<execution_context>
@/Users/amu1o5/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amu1o5/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-slides-speaker-notes/02-UI-SPEC.md
@.planning/phases/02-slides-speaker-notes/02-01-SUMMARY.md

<interfaces>
<!-- CSS classes that MUST be present in markup — all defined by Plan 01 in talks/tedx-kth/style.css -->
<!-- Executor: do not deviate from these class names or the CSS will not apply. -->

Slide wrapper classes (one per <section>):
  slide-1, slide-2, slide-3, slide-4, slide-5, slide-6,
  slide-7, slide-8, slide-9, slide-10, slide-11, slide-12

Component classes (used inside sections):
  Slide 1: .eyebrow, .title-1, .title-2, .meta
  Slide 2: .browser-frame, .browser-bar, .browser-dot (×3), .browser-url, .browser-content, .label, .heading, .subtext
  Slide 3: .terminal-text, .cursor
  Slide 4: .prompt, .command, .dot (span inside .command)
  Slide 5: .ai-cell (×4), .icon-wrap, svg, .label-main, .label-sub
  Slide 6: .manuscript-area, .arabic-deco (svg), .text-area, .name-arabic, .name-trans, .name-dates, .etymology, .word (span)
  Slide 7: .v3-label, .term (span), .row, .num-block.left, .num-block.right, .number, .label, .vs, .footer-line, .sep (span ×2)
  Slide 8: .stat-row, .stat-block.use, .stat-block.trust, .stat-num, .stat-label, .source
  Slide 9: .dragon-text, .row (span ×4), .arrow-up (span), .arrow-down (span)
  Slide 10: .reframe, .word-box.from, .word-box.to, .word, .label, .arrow-svg (svg), .footnote
  Slide 11: .term, .definition, .key (span ×2)
  Slide 12: .question, .em (span)

Palette per slide (data-background-color + class):
  slide-1:  class="deep slide-1"  data-background-color="#0a0a0a"
  slide-2:  class="deep slide-2"  data-background-color="#050505"
  slide-3:  class="deep slide-3"  data-background-color="#0a0a0a"
  slide-4:  class="deep slide-4"  data-background-color="#0a0a0a"
  slide-5:  class="deep slide-5"  data-background-color="#0f0f0f"
  slide-6:  class="deep slide-6"  data-background-color="#0a0a0a"  (gradient on element itself via CSS; data-background-color is the darkest end)
  slide-7:  class="deep slide-7"  data-background-color="#0a0a0a"
  slide-8:  class="deep slide-8"  data-background-color="#0a0a0a"
  slide-9:  class="deep slide-9"  data-background-color="#0a0a0a"
  slide-10: class="cream slide-10" data-background-color="#f5f1ea"
  slide-11: class="deep slide-11" data-background-color="#0a0a0a"
  slide-12: class="cream slide-12" data-background-color="#f5f1ea"

Reveal.js separator convention (from talks/tedx-kth.html):
  data-separator="^\n---\n$"             (horizontal slide boundary)
  data-separator-vertical="^\n--\n$"    (vertical — not used; all 12 slides are horizontal)

Speaker notes syntax:
  <aside class="notes">text</aside> inside each <section>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete tedx-kth.md with all 12 slides and speaker notes</name>

  <read_first>
    - talks/slides-markdown/tedx-kth.md (read the current 16-line probe file before replacing)
    - talks/tedx-kth/reference-material/slides.html (full file — HTML content for all 12 slides, exact SVG paths, exact copy)
    - talks/tedx-kth/reference-material/slides.md (speaker notes source — timing cues, narrative beats, stage directions)
    - .planning/phases/02-slides-speaker-notes/02-UI-SPEC.md (Speaker Notes Contract table, lines 472–493; Copywriting Contract table, lines 511–546)
    - talks/tedx-kth/style.css (confirm Plan 01 CSS classes written, so you can verify class names match)
  </read_first>

  <files>talks/slides-markdown/tedx-kth.md</files>

  <action>
REPLACE the entire contents of `talks/slides-markdown/tedx-kth.md` with the following content verbatim. This is a complete file write — the two Phase 1 probe sections are discarded.

The file uses raw `<section>` HTML blocks (not markdown headings) separated by blank-line `---` blank-line boundaries. Each section carries `data-background-color` and `class` attributes. Speaker notes live in `<aside class="notes">` inside each section.

Write the file with exactly this content:

---FILE START---

<section data-background-color="#0a0a0a" class="deep slide-1">
  <div class="eyebrow">TEDx KTH Salon · The Voice of Innovation</div>
  <div class="title-1">The next billion developers won't be blocked by syntax.</div>
  <div class="title-2">They'll be blocked by thinking.</div>
  <div class="meta">
    <span>Muhammad Ahsan Ayaz</span>
    <span>Stockholm · 04.05.2026</span>
  </div>
  <aside class="notes">
    Cold open · 0:00

    This is the official TEDx-published title. Hold on this slide for ~10 seconds before the first line, while the room settles. The audience reads the title; you do not say it aloud. Your opening line ("I want to tell you about four hours of my life I will never get back") lands while this slide is up. The signature line — "The typing has been automated. The thinking has not." — is delivered spoken-only at minute ~19, not on a slide.
  </aside>
</section>

---

<section data-background-color="#050505" class="deep slide-2">
  <div class="browser-frame">
    <div class="browser-bar">
      <div class="browser-dot"></div>
      <div class="browser-dot"></div>
      <div class="browser-dot"></div>
      <div class="browser-url">google.github.io/adk-docs</div>
    </div>
    <div class="browser-content">
      <div class="label">Agent Development Kit</div>
      <div class="heading">Build AI agents that can reason, plan, and act</div>
      <div class="subtext">Production-grade agentic framework with multi-agent orchestration, tool use, and seamless deployment to Google Cloud.</div>
    </div>
  </div>
  <aside class="notes">
    ~ 0:30

    The browser is dimmed and slightly out of focus on purpose. The audience does not need to read this slide. It is just establishing visual context for the demo story. Advance to slide 3 the moment you say "permission denied."
  </aside>
</section>

---

<section data-background-color="#0a0a0a" class="deep slide-3">
  <div class="terminal-text">PERMISSION DENIED<span class="cursor"></span></div>
  <aside class="notes">
    ~ 1:30

    Stay on this slide for the entire "generate, paste, deploy, wait, fail" sequence. The visual stillness while you describe motion is what makes the contrast hit. The slight blur and CRT-scan-line texture imply exhaustion without showing it.
  </aside>
</section>

---

<section data-background-color="#0a0a0a" class="deep slide-4">
  <div class="prompt">$</div>
  <div class="command">adk<span class="dot">.</span>web</div>
  <aside class="notes">
    ~ 3:00

    This slide should appear AFTER you say "I just needed to type three letters and a word into my terminal." The reveal is the punchline. Don't anticipate it. Hold the silence for two full seconds after the slide changes, then deliver the new "narrate their thoughts" line: "And I know what some of you are thinking right now. How does a senior engineer waste four hours like that?" Stay on this slide through that beat before continuing.
  </aside>
</section>

---

<section data-background-color="#0f0f0f" class="deep slide-5">
  <div class="ai-cell">
    <div class="icon-wrap">
      <svg viewBox="0 0 64 64">
        <path d="M14 12 L14 52 L50 52 L50 12 Z" />
        <path d="M14 12 L24 22 L50 22" />
        <line x1="22" y1="32" x2="42" y2="32" />
        <line x1="22" y1="40" x2="42" y2="40" />
      </svg>
    </div>
    <div class="label-main">Recovered a hacked blog</div>
    <div class="label-sub">A week of work · one evening</div>
  </div>
  <div class="ai-cell">
    <div class="icon-wrap">
      <svg viewBox="0 0 64 64">
        <rect x="16" y="14" width="32" height="36" rx="2" />
        <line x1="22" y1="22" x2="42" y2="22" />
        <line x1="22" y1="28" x2="38" y2="28" />
        <line x1="22" y1="34" x2="42" y2="34" />
        <line x1="22" y1="40" x2="36" y2="40" />
      </svg>
    </div>
    <div class="label-main">Replaced a CMS<br>with markdown</div>
    <div class="label-sub">$6 / month · sovereignty</div>
  </div>
  <div class="ai-cell">
    <div class="icon-wrap">
      <svg viewBox="0 0 64 64">
        <path d="M16 14 L16 50 L48 50 L48 14 Z" />
        <line x1="16" y1="20" x2="48" y2="20" />
        <line x1="22" y1="28" x2="42" y2="28" />
        <line x1="22" y1="34" x2="38" y2="34" />
        <line x1="22" y1="40" x2="42" y2="40" />
        <line x1="22" y1="46" x2="34" y2="46" />
      </svg>
    </div>
    <div class="label-main">Wrote a book<br>in 3 months</div>
    <div class="label-sub">Friction removed · thinking kept</div>
  </div>
  <div class="ai-cell">
    <div class="icon-wrap">
      <svg viewBox="0 0 64 64">
        <path d="M14 16 Q14 12 18 12 L46 12 Q50 12 50 16 L50 38 Q50 42 46 42 L28 42 L18 50 L18 42 Q14 42 14 38 Z" />
        <circle cx="24" cy="27" r="1.5" fill="currentColor" />
        <circle cx="32" cy="27" r="1.5" fill="currentColor" />
        <circle cx="40" cy="27" r="1.5" fill="currentColor" />
      </svg>
    </div>
    <div class="label-main">A marketing agent<br>called Alkhwarizmi</div>
    <div class="label-sub">A teacher in my pocket</div>
  </div>
  <aside class="notes">
    ~ 5:00

    Each cell of the grid corresponds to one beat in the spoken passage. Reveal the slide all at once when you start the "AI has given me superpowers" passage. The hand-drawn icon style is intentional — it signals that these are personal, hand-built things, not enterprise software. Hold this slide through the new loop opener: "And honestly, that should be the end of the talk... But that is not why I am standing here."
  </aside>
</section>

---

<section data-background-color="#0a0a0a" class="deep slide-6">
  <div class="manuscript-area">
    <svg class="arabic-deco" viewBox="0 0 200 260" fill="currentColor">
      <g opacity="0.7">
        <path d="M100 30 L130 70 L100 110 L70 70 Z" fill="none" stroke="currentColor" stroke-width="1.5"/>
        <path d="M100 50 L120 75 L100 100 L80 75 Z" fill="none" stroke="currentColor" stroke-width="1"/>
        <circle cx="100" cy="75" r="6" fill="currentColor"/>
        <line x1="40" y1="130" x2="160" y2="130" stroke="currentColor" stroke-width="0.8"/>
        <line x1="50" y1="135" x2="150" y2="135" stroke="currentColor" stroke-width="0.5"/>
        <path d="M100 150 L130 190 L100 230 L70 190 Z" fill="none" stroke="currentColor" stroke-width="1.5"/>
        <path d="M100 170 L120 195 L100 220 L80 195 Z" fill="none" stroke="currentColor" stroke-width="1"/>
        <circle cx="100" cy="195" r="6" fill="currentColor"/>
        <text x="100" y="252" font-family="serif" font-size="11" fill="currentColor" text-anchor="middle" font-style="italic" opacity="0.6">House of Wisdom · Baghdad</text>
      </g>
    </svg>
  </div>
  <div class="text-area">
    <div class="name-arabic">Muḥammad ibn Mūsā<br>al-Khwārizmī</div>
    <div class="name-trans">whose name became <em>algorithm</em></div>
    <div class="name-dates">c. 780 — 850 CE</div>
    <div class="etymology">
      <span class="word">al-jabr</span>: completion, balancing,<br>putting the broken parts back together.
    </div>
  </div>
  <aside class="notes">
    ~ 7:00

    Reveal this slide as you say his full name. Pause for two full seconds after "He has been working for you your whole life and you didn't know it." The geometric pattern echoes Islamic manuscript art without depicting any specific copyrighted artwork. The etymology line at the bottom returns later in Act 5 as a callback.
  </aside>
</section>

---

<section data-background-color="#0a0a0a" class="deep slide-7">
  <div class="v3-label">
    <span class="term">The Thinking Gap</span>
  </div>
  <div class="row">
    <div class="num-block left">
      <div class="number">9</div>
      <div class="label">users</div>
    </div>
    <div class="vs">vs</div>
    <div class="num-block right">
      <div class="number">~ 4,000</div>
      <div class="label">users</div>
    </div>
  </div>
  <div class="footer-line">
    Same year<span class="sep">·</span>Same hands<span class="sep">·</span>Same AI
  </div>
  <aside class="notes">
    HEART OF TALK · ~ 9:00

    UPDATED IN V3 with the branded term at the top. The label "The Thinking Gap" is what makes this concept portable. Reveal the numbers first when you say "Some of them have nine"; the term label is already visible. After you say "I started calling this the thinking gap," point to the label briefly so the audience associates the words with the visual. The 9 is muted; the ~4,000 is bright. Hold this slide all the way through the new "narrate their thoughts" line: "Now you might be thinking what I thought, the first time I noticed the gap..."
  </aside>
</section>

---

<section data-background-color="#0a0a0a" class="deep slide-8">
  <div class="stat-row">
    <div class="stat-block use">
      <div class="stat-num">84<span style="font-size:0.5em;color:var(--ink-soft)">%</span></div>
      <div class="stat-label">use AI tools</div>
    </div>
    <div class="stat-block trust">
      <div class="stat-num">29<span style="font-size:0.5em;color:var(--ink-soft)">%</span></div>
      <div class="stat-label">trust them</div>
    </div>
  </div>
  <div class="source">Stack Overflow Developer Survey · 2025 · n = 49,000+</div>
  <aside class="notes">
    ~ 11:30

    Two numbers, side by side, no chart. The 84% is the same color as the rest of the slide; the 29% is in TEDx red. The visual contrast IS the argument. Source line is small enough to be quoted accurately by anyone who screenshots, but not loud enough to compete with the numbers.
  </aside>
</section>

---

<section data-background-color="#0a0a0a" class="deep slide-9">
  <div class="dragon-text">
    <span class="row">Use <span class="arrow-up">↑</span></span>
    <span class="row">Trust <span class="arrow-down">↓</span></span>
    <span class="row">Output <span class="arrow-up">↑</span></span>
    <span class="row">Demand <span class="arrow-down">↓</span></span>
  </div>
  <aside class="notes">
    ~ 12:30

    Each line lands as you say it. If you have a clicker that supports staged reveal, animate them in one at a time. Otherwise, all four appear together. The asymmetric arrows do the work. The slide is intentionally text-only and quiet — this is the dragon section, the talk's lowest emotional point. Visual stillness amplifies it.
  </aside>
</section>

---

<section data-background-color="#f5f1ea" class="cream slide-10">
  <div class="reframe">
    <div class="word-box from">
      <div class="word">typing</div>
      <div class="label">automated</div>
    </div>
    <svg class="arrow-svg" viewBox="0 0 200 60" fill="none">
      <path d="M10 30 L180 30" stroke="#e62b1e" stroke-width="2" stroke-linecap="round"/>
      <path d="M165 18 L185 30 L165 42" stroke="#e62b1e" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
    <div class="word-box to">
      <div class="word">thinking</div>
      <div class="label">still ours</div>
    </div>
  </div>
  <div class="footnote">paraphrasing Karpathy</div>
  <aside class="notes">
    ~ 14:00

    Theme switch from dark to light. The shift in palette is intentional — it signals the talk has turned from diagnosis to insight. The strikethrough on "typing" is in TEDx red. This slide carries the new sharper negative-frame line: "And if you skip it, the speed becomes the trap. The AI will help you get to the wrong answer faster than you have ever gotten there in your life."
  </aside>
</section>

---

<section data-background-color="#0a0a0a" class="deep slide-11">
  <div class="term">Context engineering</div>
  <div class="definition">
    Be deliberate about <span class="key">what</span> you put in front of an intelligent system <span class="key">before</span> you ask it to do anything.
  </div>
  <aside class="notes">
    ~ 15:30

    Definition slide. The oversized opening quotation mark in the upper-left visually frames the line as something you are reading aloud, even though it is your own definition. The two italicized red words ("what" and "before") are the anchor points the audience should remember.
  </aside>
</section>

---

<section data-background-color="#f5f1ea" class="cream slide-12">
  <div class="question">
    What is <span class="em">worth</span><br>building?
  </div>
  <aside class="notes">
    ~ 19:00

    The final slide. The clip. The poster. Reveal this when you say "the question is what is worth building." Stay on it through the entire close, the silence, and the walk-off. The signature line — "The typing has been automated. The thinking has not." — is delivered spoken-only just before the final beat, with no slide change. Do NOT advance to a "Thank you" or contact slide. The talk ends on this question, and the lights coming back up is the punctuation.
  </aside>
</section>

---FILE END---

IMPORTANT notes for execution:
1. The `---` separators between sections MUST be preceded and followed by a blank line (the reveal.js `data-separator="^\n---\n$"` regex requires this exact whitespace).
2. Do NOT add `---` after the final section — reveal.js would create a blank 13th slide.
3. The slide-4 command is `adk<span class="dot">.</span>web` — the dot between "adk" and "web" is a literal period inside a `.dot` span (not a space character), giving it the accent color from CSS.
4. The slide-8 `%` suffix uses an inline style attribute `style="font-size:0.5em;color:var(--ink-soft)"` — this is the permitted one-off override from the CSS scoping contract.
5. The slide-10 SVG arrow hardcodes `stroke="#e62b1e"` — this is intentional (per UI-SPEC; the SVG stroke cannot use CSS variables in all render contexts).
6. Slide-6 background: the CSS in Plan 01 sets `background: linear-gradient(135deg, #1a1208 0%, #0a0a0a 100%) !important` on `.slide-6`, which overrides the `data-background-color="#0a0a0a"` for the gradient. The `data-background-color` is still needed to avoid a palette-switch flash during slide transition.
  </action>

  <verify>
    <automated>
# Count sections (expect 12)
grep -c "^<section" talks/slides-markdown/tedx-kth.md

# Count aside.notes blocks (expect 12)
grep -c 'aside class="notes"' talks/slides-markdown/tedx-kth.md

# Confirm probe content is gone
grep -c "DECK · PROBE" talks/slides-markdown/tedx-kth.md

# Confirm slide-12 closing question present
grep "worth" talks/slides-markdown/tedx-kth.md

# Confirm timing cues in notes
grep "~ 9:00\|HEART OF TALK" talks/slides-markdown/tedx-kth.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^<section" talks/slides-markdown/tedx-kth.md` → 12
    - `grep -c 'aside class="notes"' talks/slides-markdown/tedx-kth.md` → 12
    - `grep "DECK · PROBE" talks/slides-markdown/tedx-kth.md` → 0 matches (probe content removed)
    - `grep "PERMISSION DENIED" talks/slides-markdown/tedx-kth.md` → 1 match
    - `grep 'class="dot"' talks/slides-markdown/tedx-kth.md` → 1 match (slide 4 accent dot)
    - `grep "arrow-down" talks/slides-markdown/tedx-kth.md` → 2 matches (Trust ↓ and Demand ↓)
    - `grep "al-Khwārizmī" talks/slides-markdown/tedx-kth.md` → 1 match
    - `grep 'class="key"' talks/slides-markdown/tedx-kth.md` → 2 matches (slide 11 "what" and "before")
    - `grep "Do NOT advance" talks/slides-markdown/tedx-kth.md` → 1 match (NOTES-02 special direction)
    - `grep "Hold on this slide for ~10 seconds" talks/slides-markdown/tedx-kth.md` → 1 match (NOTES-02 slide-1 direction)
    - `grep "two full seconds" talks/slides-markdown/tedx-kth.md` → 1 match (NOTES-02 slide-4 direction)
    - `grep "narrate their thoughts" talks/slides-markdown/tedx-kth.md` → 1 match (NOTES-02 slide-7 direction)
    - `grep 'data-background-color="#f5f1ea"' talks/slides-markdown/tedx-kth.md` → 2 matches (slides 10 and 12 — cream slides)
    - `grep 'class="cream' talks/slides-markdown/tedx-kth.md` → 2 matches (slides 10 and 12)
    - `grep -c "---" talks/slides-markdown/tedx-kth.md` → 11 separators (12 slides = 11 boundaries, no trailing separator)
  </acceptance_criteria>

  <done>
    talks/slides-markdown/tedx-kth.md contains exactly 12 `<section>` blocks in the correct order, each with its palette class + data-background-color, all required CSS component classes present, and 12 `<aside class="notes">` blocks with timing cues and special delivery directions verbatim from the mockup. The two Phase 1 probe sections are gone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem | tedx-kth.md is a static asset read by reveal.js markdown loader; no user input |
| Inline SVG | SVG paths are hardcoded geometry from the mockup; no user-controlled content; no script execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | talks/slides-markdown/tedx-kth.md | accept | Static local file authored by executor; the only write path is the executor itself; no external write surface |
| T-02-02-02 | Information Disclosure | Speaker notes content | accept | Notes visible in reveal.js Notes window (by design); no PII or secrets in the notes content; the talk content is public domain after May 4 delivery |
| T-02-02-03 | Tampering | Inline SVG in slide-10 (hardcoded stroke="#e62b1e") | accept | Color is hardcoded intentionally (CSS var() unreliable in SVG stroke context); value matches --accent exactly; no injection risk |
| T-02-02-04 | Elevation of Privilege | HTML inline in markdown | mitigate | reveal.js Markdown plugin with `data-separator` parses the file content; inline HTML is the documented authoring method for this project (confirmed by Phase 1 probe); no script tags in markup — executor must verify no `<script>` appears in the final file |
</threat_model>

<verification>
After the task completes, run the full acceptance battery:

```bash
# Structural count
echo "=== Section count (expect 12) ===" && grep -c "^<section" talks/slides-markdown/tedx-kth.md
echo "=== Notes count (expect 12) ===" && grep -c 'aside class="notes"' talks/slides-markdown/tedx-kth.md
echo "=== Probe gone (expect 0) ===" && grep -c "DECK · PROBE" talks/slides-markdown/tedx-kth.md || echo "0"

# Slide-order spot checks
echo "=== Slide order check ===" && grep "^<section" talks/slides-markdown/tedx-kth.md | head -12

# Special effects
echo "=== Slide-3 terminal ===" && grep "PERMISSION DENIED" talks/slides-markdown/tedx-kth.md
echo "=== Slide-4 dot ===" && grep 'class="dot"' talks/slides-markdown/tedx-kth.md
echo "=== Slide-10 arrow SVG ===" && grep 'stroke="#e62b1e"' talks/slides-markdown/tedx-kth.md

# NOTES-02 special directions
echo "=== 10s hold (slide 1) ===" && grep "10 seconds" talks/slides-markdown/tedx-kth.md
echo "=== 2s silence (slide 4) ===" && grep "two full seconds" talks/slides-markdown/tedx-kth.md
echo "=== Narrate thoughts (slide 7) ===" && grep "narrate their thoughts" talks/slides-markdown/tedx-kth.md
echo "=== Do NOT advance (slide 12) ===" && grep "Do NOT advance" talks/slides-markdown/tedx-kth.md

# No script injection
echo "=== No script tags (expect 0) ===" && grep -c "<script" talks/slides-markdown/tedx-kth.md || echo "0"

# Separator count (expect 11)
echo "=== Separator count (expect 11) ===" && grep -c "^---$" talks/slides-markdown/tedx-kth.md
```
</verification>

<success_criteria>
- `talks/slides-markdown/tedx-kth.md` contains exactly 12 `<section>` elements
- Each section has the correct `class` (deep/cream + slide-N) and `data-background-color`
- Each section contains an `<aside class="notes">` with timing cue and narrative beat
- All four NOTES-02 special directions are present verbatim
- The two Phase 1 probe sections (`DECK · PROBE · DEEP` and `DECK · PROBE · CREAM`) are gone
- File is navigable end-to-end in reveal.js (12 horizontal slides, no blank 13th slide)
- No `<script>` tags in the file
- Visual elements match the mockup: cursor blink class on slide 3, `.dot` span on slide 4 command, `.key` spans on slide 11, `.em` span on slide 12
</success_criteria>

<output>
After completion, create `.planning/phases/02-slides-speaker-notes/02-02-SUMMARY.md`
</output>
