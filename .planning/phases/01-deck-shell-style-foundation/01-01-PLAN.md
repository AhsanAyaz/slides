---
phase: 01-deck-shell-style-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - talks/tedx-kth.html
autonomous: true
requirements: [DECK-01, DECK-02, DECK-03, DECK-04, DECK-05]
must_haves:
  truths:
    - "Opening talks/tedx-kth.html in a browser boots reveal.js v4.4.0 with markdown, highlight, notes, and KaTeX plugins active and no console errors"
    - "The shell loads slides-markdown/tedx-kth.md via data-markdown with ^\\n---\\n$ and ^\\n--\\n$ separators"
    - "Tailwind CDN is not loaded; black.css theme is not loaded; watermark/socials block is not present"
    - "The <body> tag carries class=\"tedx\" so style.css selectors win"
    - "talks/tedx-kth/style.css is referenced after dist/reveal.css and the highlight CSS so it has highest cascade priority"
  artifacts:
    - path: "talks/tedx-kth.html"
      provides: "Reveal.js shell entry point that the hub extractor will discover and the browser will boot"
      contains: "data-markdown=\"slides-markdown/tedx-kth.md\""
      contains_class: "class=\"tedx\""
    - path: "talks/tedx-kth.html"
      provides: "Valid <title> tag containing the TEDx-published title verbatim, pre-empting Phase 3 HUB-02 extractor pass"
      contains: "<title>The next billion developers won't be blocked by syntax. They'll be blocked by thinking.</title>"
  key_links:
    - from: "talks/tedx-kth.html"
      to: "../dist/reveal.css and ../dist/reveal.js"
      via: "<link> and <script> tags using the same relative-path convention as sibling shells"
      pattern: "\\.\\./dist/reveal\\.(css|js)"
    - from: "talks/tedx-kth.html"
      to: "talks/tedx-kth/style.css"
      via: "<link rel=\"stylesheet\" href=\"tedx-kth/style.css\" /> as the LAST stylesheet in <head>"
      pattern: "tedx-kth/style\\.css"
    - from: "talks/tedx-kth.html"
      to: "slides-markdown/tedx-kth.md"
      via: "<section data-markdown=\"slides-markdown/tedx-kth.md\" data-separator=... data-separator-vertical=...></section>"
      pattern: "data-markdown=\"slides-markdown/tedx-kth\\.md\""
    - from: "talks/tedx-kth.html"
      to: "Reveal.initialize plugins"
      via: "inline <script> calls Reveal.initialize with controls/progress/history/center flags and the four-plugin array"
      pattern: "RevealMarkdown.*RevealHighlight.*RevealNotes.*RevealMath\\.KaTeX"
---

<objective>
Author `talks/tedx-kth.html` — the reveal.js shell entry point for the TEDx KTH 2026 talk.

Purpose: This shell is the file the speaker (and the hub) opens to boot the deck. It must load reveal.js v4.4.0, the four locked plugins, the TEDx Google Fonts URL, and `talks/tedx-kth/style.css` — but it must NOT load `dist/theme/black.css`, the Tailwind CDN, the `profiles/ahsan.md` preface, or the watermark/socials block. These four deliberate deviations from the canonical sibling shell `talks/the-prompt-is-dead-long-live-the-context.html` are locked at decisions D-01, D-07, D-08, D-09 of CONTEXT.md.

Output: One file — `talks/tedx-kth.html` — that boots cleanly with zero console errors, has `<body class="tedx">`, and loads `slides-markdown/tedx-kth.md` (which Plan 03 will populate with probe sections).

This plan can run in parallel with Plan 02 (style foundation) — they touch disjoint files. The shell will reference `tedx-kth/style.css` even though that file is created by Plan 02; the link is dormant until both ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-deck-shell-style-foundation/01-CONTEXT.md
@.planning/phases/01-deck-shell-style-foundation/01-UI-SPEC.md
@.planning/phases/01-deck-shell-style-foundation/01-PATTERNS.md
@talks/the-prompt-is-dead-long-live-the-context.html
@talks/tedx-kth/reference-material/slides.html

<interfaces>
<!-- Authoritative TEDx Google Fonts URL — lift verbatim from mockup line 9 -->
<!-- talks/tedx-kth/reference-material/slides.html line 9: -->
```html
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&family=JetBrains+Mono:wght@400;500;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
```

<!-- Sibling shell plugin block — lift verbatim from talks/the-prompt-is-dead-long-live-the-context.html lines 43-47: -->
```html
<script src="../dist/reveal.js"></script>
<script src="../plugin/markdown/markdown.js"></script>
<script src="../plugin/highlight/highlight.js"></script>
<script src="../plugin/notes/notes.js"></script>
<script src="../plugin/math/math.js"></script>
```

<!-- Sibling shell Reveal.initialize block — lift verbatim from lines 49-63 (config flags locked by DECK-04): -->
```javascript
Reveal.initialize({
  controls: true,
  progress: true,
  history: true,
  center: true,

  plugins: [
    RevealMarkdown,
    RevealHighlight,
    RevealNotes,
    RevealMath.KaTeX,
  ],
});
```

<!-- Markdown loader skeleton — copy structurally from sibling lines 30-34, substitute markdown path: -->
```html
<div class="reveal">
  <div class="slides">
    <section
      data-markdown="slides-markdown/tedx-kth.md"
      data-separator="^\n---\n$"
      data-separator-vertical="^\n--\n$"
    ></section>
  </div>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author talks/tedx-kth.html shell with deviations locked in</name>
  <files>talks/tedx-kth.html</files>
  <read_first>
    - talks/the-prompt-is-dead-long-live-the-context.html (entire file, 65 lines — the canonical sibling and the source of every block to lift verbatim)
    - .planning/phases/01-deck-shell-style-foundation/01-PATTERNS.md (especially the "talks/tedx-kth.html" section: the "What to copy verbatim" table at lines 100-110 and the "What to deviate (and why)" table at lines 114-124)
    - .planning/phases/01-deck-shell-style-foundation/01-CONTEXT.md (decisions D-01, D-02, D-07, D-08, D-09 — the four deviations + body-class scoping)
    - .planning/phases/01-deck-shell-style-foundation/01-UI-SPEC.md ("Copywriting Contract" table line 161 for the exact <title> string)
    - talks/tedx-kth/reference-material/slides.html lines 1-25 (line 9 is the authoritative TEDx Google Fonts URL to lift verbatim)
  </read_first>
  <action>
Create the file `talks/tedx-kth.html` with the EXACT content below. Every line is determined — no improvisation. The shell deliberately diverges from `talks/the-prompt-is-dead-long-live-the-context.html` per decisions D-01 (no black.css), D-07 (no profiles preface), D-08 (no Tailwind CDN), D-09 (no watermark) — DO NOT add any of those even if the sibling shell has them.

Apply Prettier-compatible formatting (2-space indent, single quotes for JS, double quotes for HTML attributes, ≤80 cols where practical).

Write the file with this exact body:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />

    <title>The next billion developers won't be blocked by syntax. They'll be blocked by thinking.</title>

    <link rel="stylesheet" href="../dist/reveal.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&family=JetBrains+Mono:wght@400;500;700&family=Inter:wght@300;400;500;600&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../plugin/highlight/monokai.css" />
    <link rel="stylesheet" href="tedx-kth/style.css" />
  </head>

  <body class="tedx">
    <div class="reveal">
      <div class="slides">
        <section
          data-markdown="slides-markdown/tedx-kth.md"
          data-separator="^\n---\n$"
          data-separator-vertical="^\n--\n$"
        ></section>
      </div>
    </div>
    <script src="../dist/reveal.js"></script>
    <script src="../plugin/markdown/markdown.js"></script>
    <script src="../plugin/highlight/highlight.js"></script>
    <script src="../plugin/notes/notes.js"></script>
    <script src="../plugin/math/math.js"></script>

    <script>
      Reveal.initialize({
        controls: true,
        progress: true,
        history: true,
        center: true,

        plugins: [
          RevealMarkdown,
          RevealHighlight,
          RevealNotes,
          RevealMath.KaTeX,
        ],
      });
    </script>
  </body>
</html>
```

Implementation notes per decision:
- Per D-01: no `<link ... href="../dist/theme/black.css" id="theme" />` line. Reveal core CSS only.
- Per D-08: no `<script src="https://cdn.tailwindcss.com"></script>` line.
- Per D-07: ONE `<div class="slides">` block, ONE `<section data-markdown="slides-markdown/tedx-kth.md" ...>` — no `profiles/ahsan.md` preface section.
- Per D-09: no `<div class="watermark">...</div>` block.
- Per D-02: `<body class="tedx">` so all `body.tedx` selectors in `talks/tedx-kth/style.css` (created by Plan 02) match.
- Per the "Apply to TEDx shell" line in PATTERNS.md `<code_context>` (line 113): `<link rel="stylesheet" href="tedx-kth/style.css" />` is the LAST stylesheet in `<head>` so it wins the cascade against `dist/reveal.css` and the highlight CSS.
- Per UI-SPEC line 161 / HUB-01 / D-handles HUB-02 preemption: `<title>` is the exact TEDx-published title — copy the apostrophes verbatim (curly `'` from "won't" / "They'll" must use straight `'` so the regex `/<title>\s*([\s\S]*?)\s*<\/title>/i` in `scripts/extractSlideData.js` lines 19-20 captures it cleanly). Use `won't` and `They'll` with straight ASCII apostrophes as written above.
- Per DECK-03: `<link rel="preconnect" href="https://fonts.googleapis.com" />` AND `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />` BOTH present; the Google Fonts CSS link uses `display=swap` (it's already inside the URL via `&display=swap`).
- Per DECK-04: Reveal.initialize includes ALL four plugins — `RevealMarkdown`, `RevealHighlight`, `RevealNotes`, `RevealMath.KaTeX` — and the four flags `controls: true, progress: true, history: true, center: true`. KaTeX is included even though TEDx has no math (CONTEXT.md "Claude's Discretion" line 62: keep KaTeX for sibling-shell consistency).
  </action>
  <verify>
    <automated>grep -c "data-markdown=\"slides-markdown/tedx-kth.md\"" talks/tedx-kth.html | grep -q "^1$" && grep -q "class=\"tedx\"" talks/tedx-kth.html && ! grep -q "dist/theme/black.css" talks/tedx-kth.html && ! grep -q "cdn.tailwindcss.com" talks/tedx-kth.html && ! grep -q "watermark" talks/tedx-kth.html && ! grep -q "profiles/ahsan.md" talks/tedx-kth.html && grep -q "RevealMath.KaTeX" talks/tedx-kth.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `talks/tedx-kth.html` exists as a file
    - Contains the literal string `data-markdown="slides-markdown/tedx-kth.md"` exactly once
    - Contains the literal string `data-separator="^\n---\n$"`
    - Contains the literal string `data-separator-vertical="^\n--\n$"`
    - Contains `<body class="tedx">` (D-02)
    - Contains `<title>The next billion developers won't be blocked by syntax. They'll be blocked by thinking.</title>` exactly (UI-SPEC line 161; ASCII apostrophes only — verifiable with `grep -F`)
    - Contains `<link rel="stylesheet" href="../dist/reveal.css" />` (DECK-01)
    - Contains `<link rel="stylesheet" href="tedx-kth/style.css" />` AND that link appears AFTER the `../dist/reveal.css` link AND AFTER the `../plugin/highlight/monokai.css` link (DECK-02 / cascade priority — verify with `grep -n` line ordering)
    - Contains `<link rel="preconnect" href="https://fonts.googleapis.com" />` (DECK-03)
    - Contains `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />` (DECK-03)
    - Contains the substring `family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&family=JetBrains+Mono:wght@400;500;700&family=Inter:wght@300;400;500;600&display=swap` (DECK-03 / D-05 — exact mockup URL line 9)
    - Contains `RevealMarkdown`, `RevealHighlight`, `RevealNotes`, AND `RevealMath.KaTeX` inside the `Reveal.initialize` plugins array (DECK-04)
    - Contains `controls: true`, `progress: true`, `history: true`, `center: true` inside `Reveal.initialize` (DECK-04)
    - Does NOT contain the string `dist/theme/black.css` (D-01)
    - Does NOT contain the string `id="theme"` (D-01)
    - Does NOT contain the string `cdn.tailwindcss.com` (D-08)
    - Does NOT contain the string `tailwindcss` anywhere (D-08)
    - Does NOT contain the string `profiles/ahsan.md` (D-07)
    - Does NOT contain the string `watermark` (D-09 / DECK-05)
    - Does NOT contain the string `text-blue-500` or `@codewith_ahsan` (D-09)
    - Contains exactly ONE `<div class="slides">` block (D-07: only one slides div)
    - The `<title>` regex `/<title>\s*([\s\S]*?)\s*<\/title>/i` matches and captures non-empty content (verifies HUB-02 extractor compatibility — run: `node -e "const fs=require('fs');const d=fs.readFileSync('talks/tedx-kth.html','utf8');const m=d.match(/<title>\s*([\s\S]*?)\s*<\/title>/i);console.log(m && m[1].length > 0 ? 'OK' : 'FAIL')"`)
  </acceptance_criteria>
  <done>The file `talks/tedx-kth.html` exists, all 22 acceptance criteria above are satisfied, and Prettier formatting is applied. Browser-load smoke check is deferred to Plan 03 verification (when style.css and probe markdown also exist) and Phase 3 VERIFY-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Google Fonts CDN | Runtime third-party CSS fetch from `fonts.googleapis.com` and font files from `fonts.gstatic.com` cross supply-chain trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Google Fonts CDN response (CSS + woff2) | accept | TEDx is a static-content presentation; no PII, no auth, no destructive ops. Risk of CDN compromise injecting CSS is low and the deck is read-only. SRI is not applicable to Google Fonts (URLs return dynamic CSS keyed to UA). Plan 03 mitigates the availability dimension by self-hosting woff2 fallbacks under `talks/tedx-kth/fonts/` so the deck remains visually correct if CDN is blocked, slow, or hostile. |
| T-01-02 | Information Disclosure | Browser → Google Fonts referrer header | accept | The referrer leaks the page URL (the talk title) to Google. Acceptable: the talk is published publicly on the TEDx hub anyway; no private information is on this page. |
| T-01-03 | Denial of Service | Google Fonts CDN blocked / slow at venue | mitigate | Plan 03 declares `@font-face` rules pointing to self-hosted woff2 files in `talks/tedx-kth/fonts/`. The CSS font stacks terminate in `Georgia`, `Courier New`, and `sans-serif` so even with both CDN and self-hosted assets unavailable the deck text remains legible (per D-04 fail-soft chain). |
| T-01-04 | Spoofing | None — no auth, no session, no user identity in this phase | accept | Static asset deck; spoofing is not in scope. |
| T-01-05 | Repudiation / Elevation of Privilege | None — no audit log, no privilege boundary | accept | Static asset deck; not applicable. |
</threat_model>

<verification>
- All 22 acceptance criteria in Task 1 pass via automated grep checks
- File ordering verified: `tedx-kth/style.css` link appears after `../dist/reveal.css` link in `<head>`
- Hub extractor smoke test: `node -e "..."` one-liner above confirms `<title>` regex captures the TEDx string
</verification>

<success_criteria>
- `talks/tedx-kth.html` is the canonical TEDx shell
- Sibling-talk shells are unmodified (Plan 01 only writes one file)
- Phase 1 ROADMAP success criteria 1, 2, 5 are partially satisfied (full satisfaction requires Plans 02 and 03 — style.css and probe markdown — to ship; this plan provides the shell scaffolding they plug into)
</success_criteria>

<output>
After completion, create `.planning/phases/01-deck-shell-style-foundation/01-01-SUMMARY.md` documenting:
- The exact deviations from the sibling shell (D-01, D-07, D-08, D-09)
- The link-order decision and why `tedx-kth/style.css` is loaded last
- Any deviations from the planned content (none expected — this plan is fully specified)
</output>
