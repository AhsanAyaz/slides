---
context: default
work_unit: antigravity-cli-crash-course deck review + live-demo walkthrough
branch: antigravity-cli-yt
status: paused
last_updated: 2026-06-06T17:16:49Z
---

# Resume: Antigravity CLI deck review

<current_state>
Walking every demo in YT + conf decks on user's box. Paused mid-Demo 3 (Subagent dashboard) while investigating custom `agent.json` schema for v1.0.6. Two candidate JSON files written at `.agents/agents/{code-archaeologist,test-runner}/agent.json` — agy `/agents` panel shows only `/default`, no scan trace in cli.log. Schema rejected silently or path not auto-discovered.

Big narrative correction landed this session: `/fast` is **default** in agy, `/planning` is opt-in. Inverted in 5 spots across both decks. Demo 2 narrative survives because its prompt explicitly said "plan the fix".

Earlier 5-item deck-fix queue was applied then reverted on user instruction ("nice to have but makes video long").
</current_state>

<completed_work>

- YT deck patches committed: install URL, keyring honest framing, SSH error string, Demo 2 walkthrough rewrite (`/artifact` + `ctrl+r` both open Review)
- Both decks `/fast` → `/planning` narrative inversion (5 spots each: Three phases, mode slide, ⚠️ pitfall, cheat sheet row, common pitfalls #2) — **uncommitted**
- `concurrency-model.mp4` regenerated (gemini-cli has subagents too; agy diff is async)
- Inner-loop scenario at `talks/demos/inner-loop/` with buggy `countBreaks` + failing test
- Demo 1 + Demo 2 walkthroughs complete on user's box
- Subagent model investigated via NotebookLM + WebSearch + WebFetch: auto-spawn is canonical, custom JSON is v1.0.6 power-user surface (undocumented)
- 2 candidate agent.json profiles written — **inert**, schema unconfirmed
</completed_work>

<remaining_work>

**Active investigation:** confirm v1.0.6 custom subagent JSON schema.
- Schema tried: `{name, description, hidden, config.customAgent.{systemPromptSections, toolNames, systemPromptConfig.includeSections}}` — source: GitHub discussion #27305 (third-party reverse-engineered)
- Hypotheses to test next:
  1. Try flat `customAgent` at top level, no `config` wrapper
  2. Press `?` in `/agents` panel for shortcuts/scaffold command
  3. Inspect `~/.gemini/antigravity-cli/builtin/keep.txt`
  4. Check google-gemini/gemini-cli GitHub repo for canonical example
  5. Try snake_case field names
  6. Try minimal `{name, description}` only

**Remaining demo walkthroughs (after Demo 3 unblocked):**
- Demo 4 — Building and invoking a skill (YT ~L443)
- Demo 5 — Sandbox and permission modes (YT ~L554)
- Demo 6 — Running the migration (YT ~L688)

**Carry-forward facts to verify mid-walkthrough:**
- MCP OAuth tokens path `~/.gemini/antigravity/mcp_oauth_tokens.json` (verify at MCP demo)
- agy v1.0.6 now (was v1.0.5 earlier this session — auto-updater)
</remaining_work>

<decisions_made>

- **Reverted 5-item deck-fix queue** (permission-gate Bash, `/artifact` alt, approve keys, dirty-tree pitfall, model strip check) — user: nice to have but inflates video length
- **Inverted `/fast` ↔ `/planning` narrative** — `/fast` is default, `/planning` is opt-in deliberation. Applied to both decks.
- **Demo 3 path A** — live-build workspace agents (.json profiles) AND show auto-spawn. Both modes worth demonstrating.
- **Trust NotebookLM + WebSearch over WebFetch** for antigravity.google docs — official pages are JS-rendered, WebFetch blind
</decisions_made>

<blockers>
- **Custom agent.json schema unknown.** Workspace files at `.agents/agents/{name}/agent.json` silently not loaded. cli.log shows no scan trace.
</blockers>

## Required Reading (in order)
1. `talks/slides-markdown/antigravity-cli-crash-course-youtube.md` — YT deck with uncommitted `/fast` inversion + earlier prior-session patches now committed
2. `talks/slides-markdown/antigravity-cli-crash-course.md` — conf deck with uncommitted `/fast` inversion
3. `.agents/agents/code-archaeologist/agent.json` — schema candidate v1
4. `.agents/agents/test-runner/agent.json` — schema candidate v1

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|-----------|
| Apply nice-to-have queue items without ROI check | 5-item queue made deck longer for marginal gain → reverted | advisory | When proposing deck additions, weigh against video length budget. Conf deck has more room than YT. |
| Trust docs over shipped binary v1.0.6 | NotebookLM docs say "no agent.json"; v1.0.6 UI exposes one | advisory | Cross-check docs + shipped UI + WebSearch when both modes plausible. |
| WebFetch on antigravity.google/docs/* | All JS-rendered, returns title-only | blocking | Use NotebookLM + WebSearch; never bother WebFetch for these URLs. |
| Full `git checkout .` on dirty tree | Would destroy uncommitted deck inversions | blocking | Scope reverts to specific paths; reverse-edit when prior uncommitted changes coexist. |

## Infrastructure State

- Branch: `antigravity-cli-yt`. Last commit `7c5cd91 WIP: antigravity cli slides review` + a HANDOFF wip commit + YT deck patches commit landed this session
- Uncommitted: both deck `/fast` inversions + 2 candidate agent.json files
- agy v1.0.6 (auto-updater bumped from v1.0.5). Model: Gemini 3.5 Flash (Medium). Keychain entry present.
- Inner-loop demo replayable after `git restore talks/demos/inner-loop/slide-stats.js` (already in committed-buggy state per latest verification)

<context>
Caveman mode full active. Two decks kept in sync. Approach: walk every demo command on user's box, patch deck where reality diverges, fact-check non-obvious claims via NotebookLM + WebSearch. NotebookLM notebook id `afac2348-3723-441a-8834-d9549ac8c251`.
</context>

<next_action>
Try minimal agent.json shape — strip to just `{"name": "code-archaeologist", "description": "Maps unfamiliar code"}` — at `.agents/agents/code-archaeologist/agent.json`. Exit + restart agy. Open `/agents`. If still inert, press `?` for shortcuts. If still nothing, fetch the canonical example from google-gemini/gemini-cli GitHub repo via `gh api`.
</next_action>
