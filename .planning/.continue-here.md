---
context: default
work_unit: antigravity-cli-crash-course deck review + live-demo walkthrough
branch: antigravity-cli-yt
status: near-complete
last_updated: 2026-06-08T00:00:00Z
---

# Resume: Antigravity CLI deck review

<current_state>
ALL 6 DEMOS WALKED. Deck review effectively complete.
- Demo 4 (skills): folder+SKILL.md path fixed (`5ded8b4`)
- Demo 5 (permissions+sandbox): rewritten as rules engine + escape-route teaching beat. Live walk (Image #25) showed deny `write_file(/etc)` → agent pivots to `Bash(sudo tee /etc/foo)` → prompt appears (different action, rule doesn't cover) → sandbox closes gap (`24162c8`, `e30d789`)
- Demo 6 (migration): fabricated `agy migrate extensions` REPLACED with real `agy plugin import gemini` (or claude). Verified against shipped binary: `agy --help` lists only changelog/install/models/plugin/update; `agy plugin import [source]` = "Import plugins from gemini or claude"; binary strings "Found N extensions / Migrating extensions... / Migrated N extensions!" (`73dbf87`)
- MCP `httpUrl`→`serverUrl` verified working live on user's box.

Plugin migration VERIFIED on disk: ~/.gemini/antigravity-cli/plugins/{angular,github}/ each = plugin.json {"name":...} + mcp_config.json. github mcp_config uses serverUrl → proves migration auto-rewrites legacy url/httpUrl. Install-path TODO resolved; both decks updated with two-file layout (`67477df`).

/permissions VERIFIED: settings.json permissions.allow/deny holds action(target) rules; deny write_file(/etc) (the Demo 5 walk rule) persisted live. Real proof block added to Demo 5 in both decks (`81f1258`).

YOLO + sandbox FULLY LIVE-TESTED (`81ffefa`, `fdb4a9f`, `7ddd926`). Final verified model (agy v1.0.6 / macOS sandbox-exec):
- FS write outside active workspace → blocked "operation not permitted" (Test B, /tmp)
- FS read outside active workspace → blocked too; interactive offers bypass prompt, YOLO hard-fails (cat /etc/hosts)
- BOUNDARY = ACTIVE WORKSPACE (launch dir + --add-dir), NOT trustedWorkspaces[]. Cross-WS probe: cwd=slides, write to sibling pulsechecker (also trusted in settings) → BLOCKED. trustedWorkspaces = workspace-open-trust (permissions), separate from sandbox FS boundary. Matches agy wording "restricted to the current workspace".
- Network egress under sandbox → ALLOWED (curl HTTPS HTTP_200). macOS --sandbox does NOT contain network. Do NOT claim network blocking.
- Obvious exfil (read secret + POST) → refused by MODEL guardrail, not sandbox.
- DENY rules are a HARD FLOOR: --dangerously-skip-permissions does NOT override deny. write_file(/etc) blocked under YOLO: "Matches user-configured deny rule."
- --sandbox flag alone = containment, Docker NOT required.
- Both decks' YOLO + permissions slides rewritten with all live proofs.

Remaining (optional, soft claims, NOT live-walked):
- Migration gotchas slide: themes-don't-survive, Windows PATH `%LOCALAPPDATA%\agy\bin`
- "Keyring entries migrated by onboarding script"
- "`~/.gemini/settings.json` auto-imported on first launch"
- Capture real `agy plugin import gemini` output to pin exact deck wording
</current_state>

<completed_work>

- **Schema chase concluded** with definitive evidence from binary strings:
  - `GetAllCustomAgentConfigs` gRPC method → agy fetches custom agent configs from Antigravity backend, NOT from local files
  - Binary error string: `CustomAgentSpec is only allowed to be specified by or forwarded to SDK executables`
  - Local `.agents/agents/{name}/agent.json` path in `/agents` panel is aspirational — files silently rejected
  - Plugin scaffolded canonical schema during `agy -p "..."` call but loader still doesn't honor files
  - Canonical schema (now documented for future reference): top-level `{name, description, hidden, config.customAgent.{systemPromptSections[], toolNames[], systemPromptConfig.includeSections[]}}`; canonical agy tool names include `send_message`, `view_file`, `list_dir`, `grep_search`, `run_command`, `read_url_content`, `search_web`, `manage_task`
- **Auto-spawn verified non-deterministic** on Flash Medium tier — two prompts (multi-concern refactor, recon-pattern research) both ran serially with `/default`, zero subagent spawn
- **Demo 3 rewritten honest** (commit `4953676`) — concept-only `/agents` surface walkthrough, no fake live spawn
- **Inert experiments cleaned**: `.agents/agents/`, `.agents/skills/harness-orchestrator/`, root `AGENTS.md` (plugin artifacts) all removed
- Inner-loop demo files restored to committed-buggy state after agy refactored them during walk attempts
</completed_work>

<remaining_work>

**Demo walkthroughs:** ALL DONE (4, 5, 6 walked this session).

**Optional fact-checks (soft claims, low risk):**
- Migration gotchas: themes-don't-survive, Windows PATH `%LOCALAPPDATA%\agy\bin`
- Keyring migrated by onboarding script
- `~/.gemini/settings.json` auto-import on first launch
- Live `agy plugin import gemini` output capture

**Deck hygiene:**
- MCP OAuth token path `~/.gemini/antigravity/mcp_oauth_tokens.json` (verify if MCP demo revisited)
- `.agents/skills/ship-it/` untracked on disk (user's local test skill — leave or gitignore)
</remaining_work>

<decisions_made>

- **Demo 3 = concept-only** — no live multi-subagent terminal cut. Show `/agents` panel surface, explain `ctrl+j`/`ctrl+k`, name gating behavior. Honest framing: feature surface shipped, async-spawn matures on specific workloads + tiers.
- **Drop custom agent.json experiments** — gRPC-served, SDK-only registration. Hand-written files silently rejected.
- **Trust NotebookLM + shipped binary strings over GitHub reverse-engineering** — saved future hours
</decisions_made>

<blockers>
None active. Demo 4 unblocked.
</blockers>

## Required Reading (in order)
1. `talks/slides-markdown/antigravity-cli-crash-course-youtube.md` — YT deck, Demo 3 patched (commit `4953676`)
2. `talks/slides-markdown/antigravity-cli-crash-course.md` — conf deck, uncommitted `/fast` inversion (5 spots)

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|-----------|
| Chase agy custom agent.json schema | gRPC-served, SDK-only. Local files silently rejected. Binary string confirms `CustomAgentSpec only allowed via SDK executables`. | blocking | Do NOT write `.agents/agents/{name}/agent.json` by hand expecting registration. Future v1.0.7+ may change. |
| Fake auto-spawn in live demo | Not deterministic on Flash Medium tier. Multi-concern + recon prompts both ran serially. | blocking | Demo 3 must stay concept-only until tier/version unlocks reliable spawn. |
| WebFetch on antigravity.google/docs/* | All JS-rendered, returns title-only | blocking | Use NotebookLM + WebSearch; never bother WebFetch for these URLs. |
| Trust agy's self-narration about its sandbox | agy interactive session CLAIMED "blocks outbound internet access". Live test refuted: macOS `--sandbox` blocks filesystem writes outside trustedWorkspaces but curl https → HTTP_200 (network NOT blocked). | blocking | Do NOT add "sandbox blocks network" to decks. macOS sandbox-exec = filesystem-scoped only. Linux nsjail/Win AppContainer untested. Verify every sandbox claim with a combo FS+NET probe under `--sandbox --dangerously-skip-permissions`. |

## Infrastructure State

- Branch: `antigravity-cli-yt`. Last commits: `4953676 fix(slides): rewrite YT Demo 3 as concept-only` + `84c31ef WIP pause prior` + `595d072 YT deck patches`
- Uncommitted: conf deck `/fast` inversion only
- agy v1.0.6. Model: Gemini 3.5 Flash (Medium). Flash Medium tier does NOT auto-spawn subagents reliably on multi-concern or recon prompts.
- Inner-loop demo replayable; in committed-buggy state.

<context>
Caveman mode full active. Two decks tracked. Pattern: walk every demo on user's box, patch deck when reality diverges, fact-check via NotebookLM + binary string inspection. NotebookLM notebook id `afac2348-3723-441a-8834-d9549ac8c251`.
</context>

<next_action>
Move to Demo 4 (Building and invoking a skill, YT ~L443). First: re-read the deck section, confirm what the demo claims, then walk it on user's box. Common workflow: build a minimal skill in `.agents/skills/{name}/SKILL.md` (markdown + frontmatter), restart agy, verify it surfaces as slash command, invoke it.

Also commit conf deck `/fast` inversion if user wants to keep deck pair in sync.
</next_action>
