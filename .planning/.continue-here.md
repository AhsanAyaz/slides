---
context: default
work_unit: antigravity-cli-crash-course deck review + live-demo walkthrough
branch: antigravity-cli-yt
status: paused
last_updated: 2026-06-06T00:52:04Z
---

# Resume: Antigravity CLI deck review

<current_state>
Live-demo walkthrough of YouTube + conference deck for Antigravity CLI talk. Walking every command on user's box, fact-checking, patching deck where reality diverges. Demo 1 (install + first prompt) and Demo 2 (inner loop — read/plan/fix/run) complete. Paused between Demo 2 and Demo 3 (Subagent dashboard). YT deck has uncommitted patches; conf deck patches landed in prior session WIP commit `7c5cd91`.

Inner-loop scenario lives at `talks/demos/inner-loop/` — buggy `countBreaks` + failing test. During Demo 2, agy applied the fix; `slide-stats.js` is currently in the FIXED state (uncommitted). Revert before any re-demo.
</current_state>

<completed_work>

- Conf deck: 3 remaining gemini-cli architecture corrections (mental-model Note L158, paradigm-shift Note L177, subagent caption L368, subagent Note L373) — Done
- Both decks: install URL fix (`install.sh` → `cli/install.sh`; PowerShell to `irm | iex`) — Done
- Both decks: keyring Note rewritten to honest framing (file at `~/.gemini/antigravity-cli/antigravity-oauth-token` mode 0600 + Keychain key entry; no longer claims "no plaintext") — Done
- Both decks: SSH pitfall error string corrected to `keyring: secure lock out: dbus exception: connection failed` — Done
- `concurrency-model.mp4` regenerated — both panels show subagents, agy diff is async (gemini-cli YOU has lock badge, agy YOU has blinking cursor) — Done
- Inner-loop scenario built — `talks/demos/inner-loop/{slide-stats.js,slide-stats.test.js,README.md}` — Done
- Demo 1 walkthrough — install + first prompt verified, keyring fact-checked via `security find-generic-password` — Done
- Demo 2 walkthrough — agy read 3 files, planned, applied fix, ran test, green. `/artifact` and `ctrl+r` both open review panel — Done
</completed_work>

<remaining_work>

**Deck-fix queue (apply to BOTH youtube + conf decks):**
1. Inner-loop section: agy permission-gates Bash by default (deck implies auto-run — incorrect)
2. Add `/artifact` slash command as alternative to `ctrl+r` — both open artifact review panel
3. Approve panel key reference: `y` / `n` / `shift+a` / `p` / `esc` (replace deck's "press `y` to apply")
4. New pitfall slide: dirty working tree → agy may `git restore` to reset baseline (conditional, witnessed once)
5. Model strip check — agy shows Gemini 3.5 Flash (Medium); verify deck doesn't claim Pro elsewhere

**Remaining demo walkthroughs:**
- Demo 3 — Subagent dashboard (YT line 354)
- Demo 4 — Building and invoking a skill (YT line 441)
- Demo 5 — Sandbox and permission modes (YT line 552)
- Demo 6 — Running the migration (YT line 686)

**Carry-forward facts to verify mid-walkthrough:**
- Docs say MCP OAuth tokens at `~/.gemini/antigravity/mcp_oauth_tokens.json` (different subdir than CLI auth) — verify when reaching MCP demo
- agy v1.0.5 status strip shows `Gemini 3.5 Flash (Medium)` on user's box
- agy artifacts at `~/.gemini/antigravity-cli/brain/<uuid>/<name>.md` + `.metadata.json` — clear before re-demo for clean slate
</remaining_work>

<decisions_made>

- Scope-revert (slide-stats.js only) instead of full `git checkout .` — full revert would have destroyed uncommitted YT deck patches
- Keyring Note rewritten with honest framing — user's Keychain holds 16-byte base64 key (`Antigravity Safe Storage` / `Antigravity Key`); actual OAuth token is plaintext JSON at `~/.gemini/antigravity-cli/antigravity-oauth-token` mode 0600
- Both `/artifact` and `ctrl+r` documented as artifact-review entry points — user corrected initial assumption
- Inner-loop scenario lives in-repo for measurable red→green transition during live demo
- `concurrency-model.mp4` reframed: gemini-cli has subagents too (April 2026); agy's differentiator is async runtime, not subagent existence
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `talks/slides-markdown/antigravity-cli-crash-course-youtube.md` — YT deck with uncommitted patches; primary working file
2. `talks/slides-markdown/antigravity-cli-crash-course.md` — conf deck; keep in sync
3. `talks/demos/inner-loop/README.md` — live-demo scenario contract
4. `talks/demos/inner-loop/slide-stats.js` — currently FIXED (in-memory from agy Demo 2 run); revert before replay

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|-----------|
| Trust docs over shipped binary | Docs claimed "no plaintext tokens" — false on v1.0.5. | advisory | Run the command on user's box before patching deck. |
| Paraphrase error strings | SSH pitfall error was paraphrased from memory, diverged from canonical. | advisory | Pull exact string from NotebookLM citation or live repro. |
| Full `git checkout .` on dirty tree | Would have destroyed uncommitted YT deck patches. | blocking | Scope reverts to specific paths; never bulk-revert when deck files dirty. |

## Infrastructure State

- Branch: `antigravity-cli-yt` — `7c5cd91 WIP: antigravity cli slides review` is parent
- Uncommitted: `talks/slides-markdown/antigravity-cli-crash-course-youtube.md` (deck patches), `talks/demos/inner-loop/slide-stats.js` (agy fix from Demo 2)
- agy v1.0.5 installed at `~/.local/bin/agy`, authenticated, Keychain entry present
- Inner-loop demo: live and replayable from slides repo root after `git restore talks/demos/inner-loop/slide-stats.js`

<context>
Caveman mode full active. Two decks kept in sync. Approach: walk every demo command on user's box, patch deck where reality diverges, fact-check non-obvious claims via NotebookLM + WebSearch + WebFetch. NotebookLM notebook id `afac2348-3723-441a-8834-d9549ac8c251`.
</context>

<next_action>
Start with deck-fix queue item 1: inner-loop section permission-gating correction. Apply to both `talks/slides-markdown/antigravity-cli-crash-course-youtube.md` and `talks/slides-markdown/antigravity-cli-crash-course.md`. Then items 2-5. Then resume Demo 3 (Subagent dashboard, YT line 354).
</next_action>
